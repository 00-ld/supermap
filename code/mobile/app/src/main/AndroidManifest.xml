<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 网络 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 定位 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 相机 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 存储（targetSdk 28 及以下走旧存储权限）-->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- 相机硬件 -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true">

        <!-- SuperMap DataDownloadService 依赖已废弃的 org.apache.http.*（BasicHttpParams 等）。
             targetSdk 28 默认不打包该库，API 29+ 系统运行时不会自动加载 → NoClassDefFoundError。
             双保险：① jar 已拷进 src/main/libs 直接打进 APK；② manifest 显式声明 uses-library required=false，
             即使系统无该库也不阻断安装，走 APK 内打的 jar 即可。 -->
        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- 登录页（入口）-->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 主页 -->
        <activity
            android:name=".ui.main.MainActivity"
            android:screenOrientation="portrait" />

        <!-- 地图页 -->
        <activity
            android:name=".ui.map.MapActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- 事故列表页 -->
        <activity
            android:name=".ui.incident.IncidentListActivity"
            android:screenOrientation="portrait" />

        <!-- 任务创建页 -->
        <activity
            android:name=".ui.task.TaskCreateActivity"
            android:screenOrientation="portrait" />

        <!-- 任务详情页 -->
        <activity
            android:name=".ui.task.TaskDetailActivity"
            android:screenOrientation="portrait" />

        <!-- 现场打卡页 -->
        <activity
            android:name=".ui.checkin.CheckinActivity"
            android:screenOrientation="portrait" />

        <!-- 个人中心 & 我的任务页 -->
        <activity
            android:name=".ui.mine.MyTaskActivity"
            android:screenOrientation="portrait" />

    </application>

</manifest>
