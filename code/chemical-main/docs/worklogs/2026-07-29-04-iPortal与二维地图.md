# 阶段 5：iPortal 与二维地图

## iPortal

- `/screen` 保持本地主入口。
- 页面启动时对 iPortal 大屏做 5 秒健康检查。
- 上游正常才显示“iPortal 大屏（在线）”；异常显示“iPortal 暂不可用 · 当前本地大屏正常”。
- 当前远端端口可连接但没有有效 HTTP 响应，本地代理返回 500；源码侧不再把用户导向空白页。

## 二维地图

- 删除被浏览器拉伸的单张 `image.png` 叠加，只保留 iServer 多级瓦片。
- 恢复拖拽、滚轮、触摸、双击和缩放控件。
- 坐标输出仍统一为 4490；4547 仅供距离、缓冲、路网和空间查询适配。

## 当前外部阻塞

- 配置的二维地图服务仍为 404，必须从现有 UDBX 发布 4490 展示副本和 4547 分析服务后才能恢复正式底图。
- 本轮没有部署项目，也没有修改远端资源；仅完成故障隔离、清晰度修复代码和发布规划。

## 回滚

- 设置 `VITE_FEATURE_ISERVER_2D_TILES=false` 后可在后续实现中切回旧图层。
- iPortal 无需回滚：健康检查只影响链接展示，不改变 `/screen`。
