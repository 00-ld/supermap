# 阶段 6：验证记录

## 已通过

- `mvn -q test`：163 个后端测试通过。
- `mvn -q '-Dtest=SensorStreamTicketServiceTest,RealtimeSensorReadingServiceTest' test`：新增一次性票据与实时接入定点回归通过。
- `mvn -q -DskipTests package`：后端打包通过。
- `npm run validate:spatial-assets`：通过。
- `npm run test:unit`：4/4 通过。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- Playwright + Edge：`/#/screen` 加载成功、无 page error，截图显示 4490 主模型近景。
- 本地 HTTP/SSE 冒烟（审查整改前）：接收、重复幂等、分页和连接事件通过；整改后的短票据未做带登录态浏览器冒烟。

## 未通过/环境限制

- Python FastAPI HTTP 测试未执行：当前环境缺少 Starlette 要求的 `httpx2`，且本轮禁止新增依赖。
- `npm run build` 与显式 16 GB Node 内存两次均在 6003 个模块 transform 完成后无诊断退出 1；typecheck、lint 和单测均通过，但生产构建不能标记为通过。
- Python `compileall` 和粒子传感器校验脚本通过；完整 FastAPI HTTP 测试未执行。
- 六个附属服务 config 和二维地图服务仍未发布，无法做瓦片成功验收。
- iPortal 上游仍不返回有效 HTTP，无法做恢复后的匿名权限/资源验收。

## 运行态

- Java 后端已用最新源码在 `127.0.0.1:8081` 本地重启。
- `/healthz` 返回 200。
- 未登录申请 `/api/sensor-readings/stream-ticket` 返回 401。
- 前端开发服务和 iServer 保持原运行态。
