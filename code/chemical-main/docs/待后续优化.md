# 待后续优化清单

> 记录时间：2026-07-29  
> 原则：当前先交付可汇报的本地阶段成果；以下事项不在本轮继续扩大代码改动。

## P0：下一个阶段必须先处理

1. **场景切换原子提交**
   - 当前返回主场景的旧分支仍可能在新场景完全就绪前移除当前图层。
   - 应统一走“预检 → 暂存加载 → ready/首瓦片确认 → 原子替换”；失败只销毁暂存层，当前场景保持不动。

2. **附属场景切换并发控制**
   - 快速点击不同场景时需要全局 scene generation/token，只有最后一次选择可以提交。
   - 过期请求必须销毁自己的暂存图层，不能覆盖较新的选择。

3. **远端 18090 / 18190 服务恢复**
   - 当前网络下远端 iServer 和 iPortal 在有效 HTTP 页面之前断开或返回 500。
   - 需要在服务器侧检查进程、许可、磁盘、数据库连接、反向代理、匿名权限和资源引用。
   - 本地 `/screen` 继续作为安全入口，远端异常不得替换成本地空白 iframe。

4. **S3M 材质重新发布**
   - 当前缓存 `associateMaterialFile` 为空，并使用 CRN_DXT5 和三级纹理 LOD。
   - 需要在 iDesktopX 中按 WebGL/S3M 目标重新生成缓存，保留 PBR/普通材质、基础色、透明贴图、法线及正确色彩空间。
   - 发布前后用相同相机、曝光和光照做 iDesktop/Web 对照截图。

## P1：完成远端恢复后处理

1. 监控传感器的选中态重绘：避免 `renderMonitoringSensors()` 清空当前选中对象。
2. 设备点跨场景清理和异步 generation 校验：防止地理场景实体残留到本地 S3M 场景。
3. 扩散帧增加 `gasCode`，三维渲染前校验是否属于泄漏源 `supportedGasCodes`。
4. 空间 validator 补齐 `bindingRole/bindingMethod/equipmentType/gasCode` 枚举和气体目录校验。
5. 在 iServer 数据集中正式增加或关联：
   - `EquipmentID`
   - `ParentEquipmentID`
   - `ComponentSmID`
   - `EquipmentType`
   - `EntranceID`
   - `LeakSourceID`
   - `SensorID`
   - `FacilityID`
   - `ModelSmID`
   - `RoadNodeID`
   - `BindingRole`
   - `GasCode`
6. 用模型表面拾取或设备连接口点数据替代部分“模型轮廓中心”泄漏锚点，确保设备外表面和喷口法向可追溯。
7. 用正式 `EquipmentID` 替换当前 `ModelName + SmID 顺序 + 空间邻近` 聚合适配器，并在发布前校验一个组件只能属于一个设备。

## P2：视觉和性能精修

1. 使用 WebGL Primitive/实例化批量渲染粒子，替换大量 Entity，进一步降低 CPU 和 DOM/对象管理开销。
2. 增加不同气体的纹理噪声、软边、消散和遮挡视觉验收，不改变浓度语义。
3. 为相机连续渲染、场景竞态、未注册 SmID 拒绝、粒子预算增加组件级自动化测试。
4. 二维地图正式发布多级缓存或矢量瓦片，并完成 4490 展示 / 4547 分析往返误差验收。

## 当前可汇报结论

- 本地 4490 主场景可运行，算法 8000 和 Vite 代理健康。
- 4 个厂房出入口和 6 个设备泄漏源已进入统一空间清单，并通过 SmID、模型名称和发布轮廓校验。
- 未注册 SmID 现在明确拒绝，不再静默沿用旧泄漏源。
- 扩散算法已从大球改为有界小粒子羽流，使用有效风速、沿风距离、气体浮升/密度和实测竖向湍流参数。
- 组件拾取已具备设备级聚合查询，能返回设备编号、设备类型、组件 SmID 清单和关联传感器；正式 `EquipmentID` 字段发布列入下一阶段。
- 本地 48 帧扩散实测约 3.5 秒返回，体元逆风违规为 0，长轴沿风的非球形比例为 100%。
- 视角切换保持 ENU 米制和受控 FOV，并在 `requestRenderMode` 下连续请求渲染帧，避免跳变。
- 传感器实时链、KDE 概率云、二维地图适配和远程部署流程已有源码或文档；远端服务恢复与 S3M 重发布仍需下一阶段执行。
