# 2026-06-18 审查落实矩阵

本矩阵以 `F:\QQ\code-audit-report-2026-06-18.md` 为输入，按当前仓库 HEAD 逐项校正。它不是结项证明，而是后续修复的工作台。

## 当前已收口

| 报告问题 | 当前状态 | 仓库证据 |
| --- | --- | --- |
| CI/CD 是软门禁 | 已收口严格类型检查门禁 | `.github/workflows/ci.yml` 已执行 `npm run typecheck` 与 `npm run typecheck:strict` |
| 算法鉴权靠部署补默认 | 已改为代码默认要求鉴权 | `algorithm/api_server.py`、`algorithm/polo.py` 默认 `ALGORITHM_REQUIRE_AUTH=true` |
| `api_server.py` 与 `polo.py` 各自维护 CORS/auth 配置面 | 新增 `algorithm/service_config.py`，集中解析 `ALGORITHM_CORS_ORIGINS`、`ALGORITHM_REQUIRE_AUTH`、`ALGORITHM_API_KEY` 并统一安装 CORS/校验 API key；两个 FastAPI 入口仅保留服务名和错误包装差异，质量审计禁止配置解析逻辑回填 | `algorithm/service_config.py`、`algorithm/api_server.py`、`algorithm/polo.py`、`algorithm/tests/test_service_config.py`、`tools/code_quality_audit.py` |
| 后端业务接口大量 `ResponseEntity<Result<?>>` 双层包装 | 业务 controller 已清理，异常边界保留 HTTP 语义 | `backend/src/main/java/com/at/controller` 当前不再返回 `ResponseEntity<Result<?>>` |
| `sensor-layout` 详情和保存接口继续用匿名 `Map` 拼响应体 | 新增 `SensorLayoutResponseDTO` / `IdResponseDTO`，保留 JSON 字段名但让 controller 返回强类型 DTO；补 controller 单元测试并用质量审计禁止匿名 `Map` 回填 | `backend/src/main/java/com/at/controller/SensorLayoutController.java`、`backend/src/main/java/com/at/pojo/dto/SensorLayoutResponseDTO.java`、`backend/src/main/java/com/at/pojo/dto/IdResponseDTO.java`、`backend/src/test/java/com/at/controller/SensorLayoutControllerTest.java`、`tools/code_quality_audit.py` |
| `GasController` / `SensorController` 列表接口继续直接返回实体列表 | 新增 `GasResponseDTO` / `SensorResponseDTO`，列表接口返回 DTO 列表并保持 JSON 字段兼容；补 controller 单元测试，质量审计禁止回退到 `Result.success(list)` 直返实体 | `backend/src/main/java/com/at/controller/GasController.java`、`backend/src/main/java/com/at/controller/SensorController.java`、`backend/src/main/java/com/at/pojo/dto/GasResponseDTO.java`、`backend/src/main/java/com/at/pojo/dto/SensorResponseDTO.java`、`backend/src/test/java/com/at/controller/GasControllerTest.java`、`backend/src/test/java/com/at/controller/SensorControllerTest.java`、`tools/code_quality_audit.py` |
| `EmergencyPlanController` / `WarningHistoryController` 列表接口继续直接返回实体列表 | 新增 `EmergencyPlanResponseDTO` / `WarningHistoryResponseDTO`，列表接口返回 DTO 列表并保持前端消费字段兼容；补 controller 单元测试，质量审计禁止回退到实体列表 | `backend/src/main/java/com/at/controller/EmergencyPlanController.java`、`backend/src/main/java/com/at/controller/WarningHistoryController.java`、`backend/src/main/java/com/at/pojo/dto/EmergencyPlanResponseDTO.java`、`backend/src/main/java/com/at/pojo/dto/WarningHistoryResponseDTO.java`、`backend/src/test/java/com/at/controller/EmergencyPlanControllerTest.java`、`backend/src/test/java/com/at/controller/WarningHistoryControllerTest.java`、`tools/code_quality_audit.py` |
| `MonitorPointController` / `EmployeeController` / `UserController` 仍把实体作为响应体 | 新增 `MonitorPointResponseDTO`、`EmployeeResponseDTO`、`UserResponseDTO`，列表和写入返回 DTO；用户列表 DTO 不包含 `password` 字段；补 controller 单元测试和前端用户类型注释，质量审计禁止实体直返回退 | `backend/src/main/java/com/at/controller/MonitorPointController.java`、`backend/src/main/java/com/at/controller/EmployeeController.java`、`backend/src/main/java/com/at/controller/UserController.java`、`backend/src/main/java/com/at/pojo/dto/MonitorPointResponseDTO.java`、`backend/src/main/java/com/at/pojo/dto/EmployeeResponseDTO.java`、`backend/src/main/java/com/at/pojo/dto/UserResponseDTO.java`、`backend/src/test/java/com/at/controller/MonitorPointControllerTest.java`、`backend/src/test/java/com/at/controller/EmployeeControllerTest.java`、`backend/src/test/java/com/at/controller/UserControllerTest.java`、`frontend/src/api/sysUser.ts`、`tools/code_quality_audit.py` |
| `CarController` 列表接口继续直接返回实体列表且缺 controller 单元测试 | 新增 `CarResponseDTO`，小车列表返回 DTO 列表并保持前端字段兼容；补 `CarControllerTest` 覆盖列表、设警和重置；质量审计禁止回退到实体列表 | `backend/src/main/java/com/at/controller/CarController.java`、`backend/src/main/java/com/at/pojo/dto/CarResponseDTO.java`、`backend/src/test/java/com/at/controller/CarControllerTest.java`、`tools/code_quality_audit.py` |
| `EnvironmentReadingController` / `SimulationMonitoringController` 仍直接返回读数实体 | 新增 `EnvironmentReadingResponseDTO`、`SimulationScenarioResponseDTO`、`SensorReadingResponseDTO`，环境和仿真监测读写接口返回 DTO；不改变 `simulation` / `SIMULATED` 真实性约束；补 controller 单元测试，质量审计禁止实体响应回退 | `backend/src/main/java/com/at/controller/EnvironmentReadingController.java`、`backend/src/main/java/com/at/controller/SimulationMonitoringController.java`、`backend/src/main/java/com/at/pojo/dto/EnvironmentReadingResponseDTO.java`、`backend/src/main/java/com/at/pojo/dto/SimulationScenarioResponseDTO.java`、`backend/src/main/java/com/at/pojo/dto/SensorReadingResponseDTO.java`、`backend/src/test/java/com/at/controller/EnvironmentReadingControllerTest.java`、`backend/src/test/java/com/at/controller/SimulationMonitoringControllerTest.java`、`tools/code_quality_audit.py` |
| `SensorLayoutController` / `ImageAnalysisController` 仍存在实体直返或 DTO 内包实体 | `sensor-layout` 列表和详情改为 `SensorLayoutSummaryResponseDTO` / `SensorLayoutDetailResponseDTO`；巡检记录列表改为 `InspectRecordResponseDTO`；补 controller 测试，质量审计禁止实体响应和旧半套 DTO 回退 | `backend/src/main/java/com/at/controller/SensorLayoutController.java`、`backend/src/main/java/com/at/controller/ImageAnalysisController.java`、`backend/src/main/java/com/at/pojo/dto/SensorLayoutSummaryResponseDTO.java`、`backend/src/main/java/com/at/pojo/dto/SensorLayoutDetailResponseDTO.java`、`backend/src/main/java/com/at/pojo/dto/InspectRecordResponseDTO.java`、`backend/src/test/java/com/at/controller/SensorLayoutControllerTest.java`、`backend/src/test/java/com/at/controller/ImageAnalysisControllerTest.java`、`tools/code_quality_audit.py` |
| `sensor-layout` 删除接口文档写 REST DELETE，代码仍是伪 RPC POST `/delete/{id}` | Controller 改为 `DELETE /api/sensor-layout/{id}`，测试校验不再保留 `@PostMapping("/delete/{id}")`，质量审计禁止伪 RPC 删除路径回填 | `backend/src/main/java/com/at/controller/SensorLayoutController.java`、`backend/src/test/java/com/at/controller/SensorLayoutControllerTest.java`、`docs/api-reference.md`、`tools/code_quality_audit.py` |
| bcrypt 兼容壳 | 已移除兼容实现，只保留拒绝旧 hash 的测试样本 | `PasswordEncoderConfig` 仅注册 `argon2id`；`PasswordEncoderConfigTest` 验证 `{bcrypt}` 被拒绝 |

## 本批修复

| 报告问题 | 本批处理 | 验收点 |
| --- | --- | --- |
| `warning_history` 被当作实时读数、趋势和告警多重语义 | 新增 `simulation_scenario` 与 `sensor_reading`，把仿真场景和连续采样从告警事件中拆出来 | `deploy/mysql/init.sql` 存在两张表；后端有 `/api/simulation-monitoring/readings/*` |
| `/api/monitoring/overview` 的趋势和最新读数仍来自 `warning_history` | 后端只消费 `sensor_reading` 作为读数/趋势；无采样读数时返回空态，不再回退旧告警历史 | `MonitoringDataServiceTest` 覆盖“无采样为空”和“读数来自 sensor_reading” |
| `monitor` / `emergency` / `car` 页面仍直接把 `warning_history` 当最新读数或趋势源 | 前端改为消费 `/api/monitoring/overview` 的 `latestReadings` / `concentrationTrend`；`warning_history` 仅保留为预警历史列表 | `monitor.vue`、`emergency/index.vue`、`CarHome.vue`、`CarDetail.vue` 均接入 `monitoringData.ts` |
| 没有真实硬件数据但页面/接口容易制造“实测”幻觉 | 当前新增接口只允许 `source=simulation`、`qualityStatus=SIMULATED`，拒绝硬件/实测标签 | `SimulationMonitoringControllerTest` 覆盖拒绝 `hardware` 与 `MEASURED` |
| `inspect_record` 已被代码使用但 canonical schema 缺表 | 在 `deploy/mysql/init.sql` 中补 `inspect_record` | fresh DB 可支撑 `/api/analysis/list`、YOLO summary 写入/查询 |
| 布局明细允许同一方案重复挂同一传感器 | `sensor_layout_detail` 增加 `(layout_id, sensor_id)` 唯一约束 | canonical schema 层封住基本不变量 |
| 注释/依赖说明仍为 bcrypt 兼容背书 | 清理 `pom.xml`、`LoginMapper`、`LoginServiceImpl` 的旧口径文字；后续 `LoginMapper` 已删除，登录/注册与后台用户管理统一走 `UserMapper` | 下一轮 grep 不再把实现误判成兼容旧 hash；质量审计禁止 `LoginMapper` 回流 |
| `smart_map` 显式 `any` 债务集中在函数入口、事件和异常处理 | 将画布事件、筛选/工具/Toast、设施/入口命中、传感器编辑、逃生规划、溯源候选等入口收口到具体类型或现有动态对象边界；扩散元数据和扩散结果入口改用 `SmartMapDiffusionMeta` / `SmartMapDiffusionResult`，疏散结果入口改用 `SmartMapEvacuationRoute` / `SmartMapEvacuationBatchResult`，传感器序列包装改用 `SmartMapSeriesPoint` / `SmartMapRecord[]`，传感器缩放/浓度入口改用 `SmartSensor` / `SmartMapSensorSeriesRecord`，告警阈值入口改用 `SmartMapSensorGasThreshold`；`frontend/src/views/smart_map/index.vue` 已移除 `AnyRecord` 兜底别名 | 质量审计禁止 `AnyRecord` 兜底别名和已收窄入口回退 |
| `smart_map` 风险网格和标准布点仍复用 `AnyRecord` 动态对象 | 新增 `SmartMapRiskGridCell` / `SmartMapRiskGridLevel`，风险网格、热力统计和标准布点生成改用明确结构；质量审计禁止 `riskGrid` 与 `generateStandardBasedSensorLayout` 回退到 `AnyRecord` | `frontend/src/views/smart_map/useSmartMapSensorPlacementRules.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 文档仍混用旧鉴权默认、双层响应和默认账号口径 | 清理 clean 文档中的算法鉴权默认、Java 响应契约、算法兼容字段和默认账号描述 | `algorithm/README.md`、`docs/coding-standards.md`、`docs/technical-route-to-deployment.md`、`docs/login-startup-troubleshooting.md`、`docs/changelog.md` 已按当前代码与 schema 改写 |
| 溯源观测只看当前动画帧，当前帧安静时会误过滤有效传感器 | 前端和算法默认使用全时段峰值信号，并保留当前帧/峰值加权作为显式专家选项 | `smart_map` 输出 `observationSignalMode`、`signal/currentConcentration/sampledPeak`；`inversion_dataset.py`、`grid_search.py`、`particle_filter.py` 同步消费；`python -m algorithm.inversion.test_observation_signal` 与 `python -m algorithm.inversion.validate_particle_filter` 通过 |
| 溯源粗搜/粒子滤波只看浓度形状时，可能接受同一风轴上游偏移的伪源点 | 粗搜、解析精修和粒子滤波统一消费观测到达时间；新增风向投影绝对/相对到达时间残差和上风向信号惩罚，粗搜到达时间残差按观测信号加权且组合分数要求到达时间受浓度形状支撑，`topK` 改为候选上限而不是弱候选凑数目标，粒子滤波诊断返回 `arrivalTimeConstraint`；这是仿真观测约束增强，不新增真实验证样本 | `algorithm/inversion/grid_search.py`、`algorithm/inversion/plume_losses.py`、`algorithm/inversion/particle_filter.py`、`algorithm/inversion/test_wind_timing_constraints.py`、`tools/code_quality_audit.py`；`.venv\\Scripts\\python.exe -m algorithm.inversion.test_wind_timing_constraints` 与 `.venv\\Scripts\\python.exe -m algorithm.inversion.validate_particle_filter` 通过 |
| 溯源精修无候选区时硬造 fallback 候选 | 精修阶段不再生成候选区；缺少候选区或候选中心点时按输入契约错误暴露 | `source_inversion.py` 拒绝空候选区；`api_server.py` 对算法输入错误返回 HTTP 400；`python -m algorithm.inversion.test_candidate_validation` 覆盖 |
| 旧疏散链主路线失败后偷偷退化到 `endPoint` fallback 路线 | 删除 `calculate_gas_and_path` 的失败后替换路线；前端只在业务结果成功且路径非空时展示完成态 | `gas_diffusion_astar.py` 保留 blocked 状态和空路径；`emergency/index.vue` 检查 `data.success/pathInfo.status/escapePath.length`；`python -m algorithm.tests.test_path_hazard_avoidance` 覆盖 |
| 扩散核心对未知气体、非法稳定度和坏数值静默 fallback | 未知气体/非法稳定度/格式错误数值改为输入契约错误；只允许字段缺失时使用明确默认值 | `gaussian_plume.normalize_stability`、`phase1_diffusion.get_gas_by_id/parse_float` 抛 `ValueError`；`python -m algorithm.diffusion.test_input_contract` 覆盖 |
| 逃生路径会从建筑/出口点直连道路，视觉上像穿越非道路区域 | D* Lite 起点和出口先投影到最近可通行道路边，再在道路图上搜索并返回 `roadAccess` 解释字段 | `algorithm/planning/dstar_lite.py` 新增 `resolve_road_access`；`python -m algorithm.planning.test_dstar_lite` 覆盖离路入口/出口投影 |
| README/部署/前端/架构文档仍传播旧运行事实 | 删除手工复制后端 jar、旧仓库 URL、不存在的人员种子文件和“已接入实时采样存储”的表述 | `README.md`、`deploy/README.md`、`frontend/README.md`、`docs/architecture.md` 按当前 compose、数据边界和文件树改写 |
| `docs/architecture.md` 仍有措辞暗示真实传感器/小车采样链路 | 将“气体实时监控”和“传感器/小车 → Java 后端”改为“气体监测与仿真读数”“仿真读数/手工观测/巡检图片素材”；小车调度说明明确当前不具备真实车载传感器连续采样链路，质量审计禁止旧表述回填 | `docs/architecture.md`、`tools/code_quality_audit.py` |
| Nginx 配置曾维护不存在的 `/ws/` WebSocket 通道 | 当前 `deploy/nginx/default.conf` 不再代理 `/ws/`；仓库审计禁止部署配置或文档重新声明未实现 WebSocket 通道 | `deploy/nginx/default.conf`、`deploy/README.md`、`tools/audit_repository.py` |
| `sensor_data.sql` 已废弃但工具和文档仍当作 canonical 输入 | 默认校验链路只检查 `deploy/mysql/init.sql`；legacy seed 从 manifest canonical 列表移出，生成脚本默认 dry-run | `tools/validate_real_sensor_layout.py`、`tools/generate_real_sensor_seed.py`、`db/manifest.json`、`docs/real-dom-map-sensor-layout.md` 已同步口径 |
| `db/migrations/` 只有 README，真实迁移文件仍散在 `deploy/mysql` | 新增规范迁移脚本 `db/migrations/001_add_user_role.sql`；`deploy/mysql/migration_add_role.sql` 保留为部署兼容入口；manifest、db README 和迁移 README 同步，质量审计禁止迁移目录退回空壳 | `db/migrations/001_add_user_role.sql`、`db/manifest.json`、`db/README.md`、`db/migrations/README.md`、`tools/code_quality_audit.py` |
| canonical schema 中核心表没有统一来源/创建/更新时间字段 | `deploy/mysql/init.sql` 为 `patrol_car`、`warning_history`、`sensor_reading`、`monitor_point`、`employee`、`inspect_record` 等核心表补来源和审计字段；新增 `db/migrations/002_add_core_audit_columns.sql` 供已有库幂等补列；质量审计检查 fresh schema 与迁移资产 | `deploy/mysql/init.sql`、`db/migrations/002_add_core_audit_columns.sql`、`db/manifest.json`、`tools/code_quality_audit.py` |
| 文档仍传播旧认证接口 `/api/user/login` | 认证文档统一到后端真实入口 `/api/auth/login` 与 `/api/auth/register`；历史 changelog 标清旧版问题和当前口径 | `docs/api-reference.md`、`docs/login-startup-troubleshooting.md`、`docs/changelog.md` |
| `sensor-placement-guide.md` 仍把 179 点写成当前验收结论 | 将其降级为历史候选布设方案，并声明当前 canonical seed 是 `deploy/mysql/init.sql` 的 61 个真实 DOM 点位 | `docs/sensor-placement-guide.md` 顶部状态说明、汇总表和结论已改写 |
| 前端 Sass `@import` 废弃警告 | 全局变量注入和主样式入口迁移到 Sass 模块语法 `@use` | `frontend/vite.config.ts`、`frontend/src/styles/index.scss` 不再使用 `@import` |
| 员工/用户后台仍会把空更新或重复用户名掩成成功/500 | 员工更新 0 行改为业务错误；管理员创建重复用户名在 insert 前和唯一键竞态时都返回明确参数错误语义 | `EmployeeServiceTest`、`UserServiceTest` 覆盖缺失员工更新和重复用户名 |
| 监测点 CRUD 与详情页不闭合 | 详情页改为按 `monitor_point` 表判断实体是否存在，移除 1..4 写死名称兜底；新增监测点按用户输入名称入库；`/monitor` 根入口回到监测点目录，`/monitor/:id` 才进入详情；无视频/传感器绑定时明确显示未绑定，气体与环境读数标为 `monitoring/overview` 全局采样参考 | `frontend/src/layout/monitor.vue`、`frontend/src/views/thing/monitor_history/index.vue`、`frontend/src/router/routes.ts`、`tools/code_quality_audit.py` |
| `MonitorPoint` 领域对象仍只有名称，无法表达监控源、传感器/视频绑定或坐标 | `monitor_point` canonical schema 和迁移新增区域、来源类型、传感器编号、摄像头 URL、坐标与质量状态；API/前端详情页消费这些字段，未绑定仍显式空态，不冒充真实硬件链路 | `deploy/mysql/init.sql`、`db/migrations/003_extend_monitor_point_semantics.sql`、`backend/src/main/java/com/at/pojo/MonitorPoint.java`、`frontend/src/api/monitorPoint.ts`、`frontend/src/layout/monitor.vue`、`tools/code_quality_audit.py` |
| `dataset-sources.md` 把不存在脚本/占位来源写成事实 | 改成当前存在的扩散验证脚本，Prairie Grass 来源不再保留个人本地路径占位；仓库审计新增防回归检查 | `docs/dataset-sources.md`、`datasets/samples/prairie_grass/README.md`、`tools/audit_repository.py` |
| 启动/运行文档仍有旧命令口径 | 算法服务手动运行统一为 `uv run`；后端 README 明确 local profile 与 Dockerfile 源码构建路线；根 README 验证命令去重并对齐当前脚本 | `algorithm/README.md`、`backend/README.md`、`docs/login-startup-troubleshooting.md`、`README.md` |
| `startup.bat` 仍用裸 `python -m uvicorn` 启动算法服务，与 `uv run` 运行口径分叉 | 一键启动脚本增加 `UV_CMD` 解析与检查，算法 API / YOLO API 均通过 `uv run --python "%PYTHON_CMD%" uvicorn ...` 启动；质量审计禁止回退到裸 `python -m uvicorn` | `startup.bat`、`README.md`、`tools/code_quality_audit.py` |
| 后端手动启动文档仍写裸 `mvn spring-boot:run`，会默认命中 `prod` 配置 | 根 README、后端 README、开发指南和 changelog 统一改为 `mvn -Dspring-boot.run.profiles=local spring-boot:run`；质量审计禁止本地启动文档回退到裸命令 | `README.md`、`backend/README.md`、`docs/development-guide.md`、`docs/changelog.md`、`backend/src/main/resources/application-local.yml`、`tools/code_quality_audit.py` |
| 本地启动排查文档仍混用旧 MySQL 端口且漏写算法密钥 | `startup.bat --check` 增加 YOLO 和共享配置入口检查；根 README 与登录排查文档对齐 `127.0.0.1:3307`；手动 YOLO/后端流程补 `ALGORITHM_API_KEY`，避免默认鉴权下代理调用失败 | `startup.bat`、`README.md`、`docs/login-startup-troubleshooting.md`、`tools/code_quality_audit.py` |
| `start.bat` 与 `startup.bat` 容易再次分叉成两套启动口径 | `start.bat` 保持兼容薄入口，只转发到 `startup.bat`；质量审计禁止它重新写入 `uvicorn/mvn/npm/docker` 等实际启动逻辑 | `start.bat`、`startup.bat`、`tools/code_quality_audit.py` |
| 核心算法“5 次真实性验证”缺证据矩阵 | 新增真实性验证报告，把真实公开实验、仿真回归、失败边界和未满足项分开列出，并用质量审计守住文档存在性 | `docs/algorithm-verification-report.md`、`tools/code_quality_audit.py` |
| `models/` 缺少模型版本治理入口，YOLO 结果不含结构化模型版本 | 新增模型 manifest；YOLO 服务健康检查和识别结果返回 `modelId/modelVersion/modelPath`；前端识别结果显示模型版本 | `models/manifest.json`、`algorithm/polo.py`、`frontend/src/views/yolo/Home.vue`、`frontend/src/views/smart_map/useSmartMapYolo.ts` |
| 小车管理页没有直接调用 YOLO 的上传入口 | 在厂区实时监测页签新增 JPG/PNG 巡检图片导入、后端识别、结果图、人数、耗时和模型版本展示；视频识别仍不冒充已支持 | `frontend/src/views/car/CarHome.vue` |
| YOLO optional extra 与 8100 模型推理服务口径断裂 | README/development-guide 明确本地 YOLO 必须 `uv sync --extra yolo`；`项目总体要求` 将当前 YOLO 端口改为 8001，并把 8100 标为未来统一推理服务规划 | `README.md`、`docs/development-guide.md`、`docs/项目总体要求.md`、`tools/code_quality_audit.py` |
| 前端 `.env.production` 继续声明不会被浏览器生产运行时消费的固定服务器地址 | 生产 env 只保留 `/api`、`/algorithm-api` 和 iPortal 相对路径；`VITE_SERVE` / `VITE_ALGORITHM_SERVE` 明确限定为 `.env.development` 的 Vite dev proxy 配置，质量审计禁止回填到生产 env 或浏览器 env 类型 | `frontend/.env.production`、`frontend/src/vite-env.d.ts`、`frontend/README.md`、`docs/development-guide.md`、`tools/code_quality_audit.py` |
| `docs/README.md` 文档自检命令默认误报 | 删除宽泛 `localhost|Manage|Back|python/` grep，改为定向查陈旧口径/占位符/不存在脚本，并纳入质量审计防回归 | `docs/README.md`、`tools/code_quality_audit.py` |
| `项目总体要求` 把不存在的验证/部署/DB 资产写成当前口径 | 将 `datasets/raw`/`processed`、测试分层目录标成目标/本地忽略目录；部署文档改为现有 `deploy/README.md`；DB canonical source 改为 `deploy/mysql/init.sql` | `docs/项目总体要求.md`、`tools/code_quality_audit.py` |
| 前端算法 API 封装返回裸 `AlgorithmResponse` 与 `unknown` 请求泛型 | 为扩散、溯源、疏散、验证报告、气体类型和引擎任务定义返回类型；同步新增 `AlgorithmTraceFields` / `AlgorithmIdentityMeta` / `AlgorithmRuntimeMeta` / `AlgorithmInputSummary`，让前端类型承接 Obsidian 工程化返回字段，并用质量审计禁止退回裸响应 | `frontend/src/types/api.ts`、`frontend/src/api/algorithm.ts`、`tools/code_quality_audit.py` |
| `algorithmClient` 曾把传输失败 resolve 成伪 `AlgorithmResponse`，与普通请求层 reject 语义分裂 | 当前 `algorithmClient` 使用 `createHttpClient` 的 reject 语义，失败时抛出带 `response/cause` 的 Error；质量审计禁止回退到 `as AlgorithmResponse` 或 `success:false` 假响应 | `frontend/src/api/algorithmClient.ts`、`frontend/src/utils/request.ts`、`tools/code_quality_audit.py` |
| `monitor_history` 页面绕过 API 层直调 `/history/list`、`/history/delete`，失败分支自行解释响应封套 | 新增 `reqDeleteWarningHistory()`，页面列表/删除统一走 `frontend/src/api/warningHistory.ts` 和共享 `ApiResult`；质量审计禁止页面重新直接 import `request` 或直调 `/history/*` | `frontend/src/api/warningHistory.ts`、`frontend/src/views/thing/monitor_history/index.vue`、`tools/code_quality_audit.py` |
| 车载与首页页面继续分散直调 `/history/list`、`/history/add` | `warningHistory.ts` 补 `reqAddWarningHistory()`；`CarDetail`、`CarHome`、首页统一消费 warning history API 层，审计禁止这些路径回填页面 | `frontend/src/api/warningHistory.ts`、`frontend/src/views/car/CarDetail.vue`、`frontend/src/views/car/CarHome.vue`、`frontend/src/views/home/index.vue`、`tools/code_quality_audit.py` |
| 首页核心统计继续把“在运设备数/设备在线率”写成模拟成品态，且“实时监测”快捷入口跳到 YOLO route name | 首页接入 typed `/monitoring/overview`，监测点总数/在线监测点来自后端监测概览；企业/入园/VOCs/公告继续显式模拟；“实时监测”快捷入口改为 `/monitor`，质量审计禁止退回 `SystemSetting` 和旧模拟设备卡 | `frontend/src/api/monitoringData.ts`、`frontend/src/views/home/index.vue`、`tools/code_quality_audit.py` |
| `frontend/src/views/home/README.md` 仍按旧首页能力写设备数、设备在线率、数字园区快捷入口和公告后端接口 | 主页模块规范改为当前事实：企业/入园为模拟，监测点指标来自 `monitoring/overview`，公告为示例且无详情跳转，实时监测入口为 `/monitor`，数字园区当前不在主页快捷按钮中；质量审计禁止旧规范回填 | `frontend/src/views/home/README.md`、`tools/code_quality_audit.py` |
| `frontend/src/views/home/README.md` 的接口建议表仍保留不存在的 `/api/dashboard/notices` 等 dashboard 聚合路径 | 主页 README 的接口表改为当前事实：`/api/monitoring/overview` 与 `/api/history/list`；公告明确无后端接口，未来 dashboard 聚合接口必须先落 typed API 再写入页面/文档；质量审计禁止 `/api/dashboard/*` 伪接口回填 | `frontend/src/views/home/README.md`、`tools/code_quality_audit.py` |
| 首页公告入口和 `/acl` 根路由存在天然坏链 | 首页公告降级为不可点击的“示例公告”，禁止回填 `AnnounceList/AnnounceDetail`；`/acl` 根路由重定向到存在的 `/acl/role`，禁止退回 `/acl/user` | `frontend/src/views/home/index.vue`、`frontend/src/router/routes.ts`、`tools/code_quality_audit.py` |
| `/car/home` 与 `/yolo` 的 route name 仍沿用 `EquipmentInspect` / `SystemSetting` 历史模板名 | 保持 URL 不变，仅将内部 route name 改为 `CarInspectionHome` / `YoloInspection`，同步首页跳转和小车页 active 判断；质量审计禁止旧 route name 回填 | `frontend/src/router/routes.ts`、`frontend/src/views/home/index.vue`、`frontend/src/views/car/CarHome.vue`、`tools/code_quality_audit.py` |
| `carStore.ts` 直接访问 `/car/getAllCars`、`/car/setWarning`、`/car/resetStatus`，状态层兼任 API owner | 新增 typed `frontend/src/api/car.ts`，store 只消费 `reqCarList/reqSetCarWarning/reqResetCarStatus`；质量审计禁止 store 重新 import `request` 或直写 `/car/*` 路径 | `frontend/src/api/car.ts`、`frontend/src/store/carStore.ts`、`tools/code_quality_audit.py` |
| `/analysis/person`、`/analysis/list`、`/analysis/delete` 在 YOLO 页面、小车页和 `smart_map` 中分散直调 | 新增 typed `frontend/src/api/analysis.ts`，统一承接 YOLO 识别结果、巡检记录列表/删除和 Obsidian 工程化元数据字段；三个页面改走 `reqAnalyzePersonImage/reqInspectRecordList/reqDeleteInspectRecord`，审计禁止路径回填页面 | `frontend/src/api/analysis.ts`、`frontend/src/views/yolo/Home.vue`、`frontend/src/views/car/CarHome.vue`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| YOLO 大屏“当前识别/识别耗时”首屏未接入后端汇总，容易和模拟运营图混在一起 | 接入 typed `/analysis/summary`，首屏人员数和耗时优先来自后端最新巡检记录，并显示“来源：后端巡检记录 / 暂无后端巡检记录”；风险分布、巡检趋势、安全天数继续保留模拟标签，不冒充真实运营指标 | `frontend/src/api/yoloSummary.ts`、`frontend/src/views/yolo/Home.vue`、`tools/code_quality_audit.py` |
| 前端 admin-only 写操作入口对所有登录用户裸露，角色 UI 和后端权限脱节 | 用户 store 解码 JWT `role/username`，提供 `isAdmin/currentRole/currentUsername/displayName`；顶部头像从 token 身份派生，不再硬写“用户”；人员管理菜单和首页人员管理快捷入口仅 admin 可见，YOLO 上传/删除和小车预警写操作在非 admin 下禁用或隐藏；后端 `RequiresRole` 仍是最终权限边界，质量审计防回退 | `frontend/src/store/modules/user.ts`、`frontend/src/layout/index.vue`、`frontend/src/views/home/index.vue`、`frontend/src/views/yolo/Home.vue`、`frontend/src/views/car/CarHome.vue`、`tools/code_quality_audit.py` |
| 请求层、共享 API 类型、算法 API 和小车 store 容易重新引入显式 `any` | 质量审计新增热点检查，禁止 `request.ts`、`types/api.ts`、`api/algorithm.ts`、`carStore.ts` 重新出现显式 `any`、`Record<string, any>` 或 `post<any>` 类调用 | `frontend/src/utils/request.ts`、`frontend/src/types/api.ts`、`frontend/src/api/algorithm.ts`、`frontend/src/store/carStore.ts`、`tools/code_quality_audit.py` |
| YOLO 识别结果契约与需求文档错位 | YOLO 响应升级为 `yolo-detection/v1`：返回 bbox、confidence、frameIndex、capturedAt、carId 空值来源、模型身份；同时按 Obsidian 工程化笔记补 `requestId`、`inputSummary`、`algorithm`、`runtime`、`warnings`、`grayRelease` 和结构化日志，Java 代理透传 `X-Request-Id` | `algorithm/polo.py`、`algorithm/response_utils.py`、`backend/src/main/java/com/at/controller/ImageAnalysisController.java`、`frontend/src/views/yolo/Home.vue`、`frontend/src/views/car/CarHome.vue`、`frontend/src/views/smart_map/useSmartMapYolo.ts`、`tools/code_quality_audit.py` |
| YOLO `/api/health` 仍是普通成功封套，和算法识别/错误响应的工程化 trace 契约不一致 | YOLO health 改为携带 `requestId/inputSummary/algorithm/runtime/warnings/grayRelease` 和直接兼容字段 `algorithmVersion/configVersion/costMs/worker`，质量审计禁止退回裸 health payload | `algorithm/polo.py`、`tools/code_quality_audit.py` |
| YOLO 失败响应仍只返回普通错误封套，缺少可复现/可排错元数据 | 新增 `yolo_error_response()`，鉴权/模型缺失/图片类型、大小、解码、结果编码等失败均返回同一 trace contract：错误响应 `data` 内包含 `requestId`、`algorithm`、`runtime`、`inputSummary`、`warnings`、`grayRelease`、`error`，并记录 `yolo_inference_failed` 结构化日志 | `algorithm/polo.py`、`tools/code_quality_audit.py` |
| YOLO health 已有直接 trace 字段，但识别成功/失败 payload 仍缺少直接兼容字段 | YOLO 识别成功和 `yolo_error_response()` 的 `data` 同步补 `algorithmVersion/configVersion/costMs/worker`，并让质量审计要求 health、success、error 三类 payload 都暴露这些字段 | `algorithm/polo.py`、`tools/code_quality_audit.py` |
| `/api/analysis/person` 用 `recordSaved` 表达识别成功但入库失败 | 改为“识别并留痕”原子语义：算法识别成功但巡检记录保存失败时后端返回 `500 巡检记录保存失败`，前端不再消费 `recordSaved` 半成功字段，质量审计禁止回归 | `backend/src/main/java/com/at/controller/ImageAnalysisController.java`、`frontend/src/views/yolo/Home.vue`、`frontend/src/views/car/CarHome.vue`、`tools/code_quality_audit.py` |
| 主算法服务结果缺少可复现/可排错元数据 | `api_server.py` 对主算法入口做兼容式增强：保留原业务字段，同时追加 `requestId`、`algorithm`、`runtime`、`inputSummary`、`warnings`、`error`，并记录不含完整输入的结构化完成日志；质量审计禁止回归裸结果 | `algorithm/api_server.py`、`tools/code_quality_audit.py` |
| 主算法服务失败结果仍缺少可复现/可排错元数据 | `algorithm_error_response()` 改为和成功响应同一 trace contract：错误响应 `data` 内包含 `requestId`、`algorithm`、`runtime.costMs/worker`、`inputSummary`、`warnings`、`error`，并记录 `algorithm_task_failed` 结构化日志；输入契约错误、算法执行异常和验证报告缺失都走该路径 | `algorithm/api_server.py`、`tools/code_quality_audit.py` |
| 主算法服务返回仍缺少工程化学习笔记里的直接排错字段 | 成功、失败和健康检查响应的 `data` 直接暴露 `algorithmVersion`、`configVersion`、`costMs`、`worker`、`grayRelease.rollbackTarget`，同时保留结构化 `algorithm/runtime/inputSummary/warnings`；前端共享类型、专项测试和质量审计同步锁住 | `algorithm/api_server.py`、`algorithm/tests/test_api_server_http_semantics.py`、`frontend/src/types/api.ts`、`tools/code_quality_audit.py` |
| 主算法服务 HTTP/未捕获异常仍可能返回 `data=null`，和算法任务错误响应的工程化 trace 契约不一致 | FastAPI HTTP 异常和全局异常处理器改走 `algorithm_error_response()`，统一返回 `requestId/inputSummary/algorithm/runtime/warnings/grayRelease`，并用测试覆盖鉴权配置错误的 trace data | `algorithm/api_server.py`、`algorithm/tests/test_api_server_http_semantics.py`、`tools/code_quality_audit.py` |
| 旧算法端点会把 `success=false` 业务失败包成 HTTP 200 | `/api/gas-path` 与 `/api/time-series` 已从公开 FastAPI 服务面移除；旧扩散 + A* 实现仅作为内部回归对象保留，不再作为服务 API 暴露；质量审计禁止旧路由和旧端点函数回填 `api_server.py` | `algorithm/api_server.py`、`algorithm/tests/test_api_server_http_semantics.py`、`tools/code_quality_audit.py` |
| 溯源 payload 没有单一 schema，靠 `observationPayload/exportPayload/pinnExportPayload/trainingConfig` 兼容壳猜字段 | 无条件拒绝旧别名：`exportPayload`、`pinnExportPayload`、`trainingConfig` 不再能通过环境开关恢复；当前 schema 继续支持 `observationPayload/refinementConfig/activeSensors`，并新增契约测试和质量审计 | `algorithm/inversion/inversion_dataset.py`、`algorithm/inversion/particle_filter.py`、`algorithm/inversion/source_inversion.py`、`algorithm/inversion/test_candidate_validation.py`、`tools/code_quality_audit.py` |
| 前端 TypeScript 源码仍可能重新引入显式 `any` | 当前 `frontend/src` 下 `.ts` / `.vue` 全量无 `any`、`Record<string, any>`、`as any`、`post<any>` 类命中；质量审计从热点文件扩展为全前端源文件门禁 | `tools/code_quality_audit.py` |
| 普通 `npm run typecheck` 仍能暴露 `smart_map` composable 入参逆变不匹配 | 智能地图调用边界改为显式泛型和 typed adapter，设备图片 composable 泛型化，候选区到通用记录的转换先经 `unknown`；`npm run typecheck` 与 `npm run typecheck:strict` 均通过 | `frontend/src/views/smart_map/index.vue`、`frontend/src/views/smart_map/useSmartMapDeviceImage.ts`、`frontend/src/views/smart_map/useSmartMapObservationBuilders.ts` |
| 旧疏散链缺少 `startPoint/leakPoint` 时使用默认点位继续算 | `calculate_gas_and_path()` 与 `simulate_time_series()` 改为强制读取请求里的 `startPoint`、`leakPoint`，缺失或坐标不完整时返回失败结果；专项测试与质量审计禁止默认坐标回归 | `algorithm/planning/gas_diffusion_astar.py`、`algorithm/tests/test_path_hazard_avoidance.py`、`tools/code_quality_audit.py` |
| `emergency` 与 `smart_map` 分别调用旧/新扩散疏散链 | `emergency/index.vue` 已迁到 `/api/diffusion/simulate` + `/api/planning/evacuation`，使用和 `smart_map` 一致的新扩散帧、D* Lite、`roadAccess` 语义；前端 API 层删除 `/api/gas-path`、`/api/time-series` wrapper，并由质量审计禁止回填 | `frontend/src/views/emergency/index.vue`、`frontend/src/api/algorithm.ts`、`tools/code_quality_audit.py` |
| `algorithm/` 测试/验证入口依赖 `sys.path.insert` 或双导入 fallback | 清理 4 个高价值验证入口的路径注入、扩散/溯源主链生产模块双导入 fallback，并继续删除扩散/溯源测试入口中的 `except ImportError` 兼容壳；10k 重规划 benchmark、API 服务入口和路径危险规避测试均改为直接导入 `algorithm.planning` 实现，包根兼容壳已删除；质量审计禁止这些已清理文件回退到 `sys.path`、双导入或内部依赖包根兼容壳 | `algorithm/tests/test_analytic_inversion.py`、`algorithm/planning/test_dstar_lite.py`、`algorithm/inversion/test_eki_inversion.py`、`algorithm/inversion/validate_particle_filter.py`、`algorithm/diffusion/test_gaussian_validation.py`、`algorithm/diffusion/test_input_contract.py`、`algorithm/diffusion/test_physical_invariants.py`、`algorithm/diffusion/test_real_prairie_grass.py`、`algorithm/inversion/test_candidate_validation.py`、`algorithm/inversion/test_observation_signal.py`、`algorithm/diffusion/conditioned_advection.py`、`algorithm/diffusion/phase1_diffusion.py`、`algorithm/inversion/forward_model.py`、`algorithm/inversion/grid_search.py`、`algorithm/inversion/plume_losses.py`、`algorithm/deep_learning/gas_surrogate.py`、`algorithm/deep_learning/validate_btex_real_data.py`、`algorithm/planning/gas_diffusion_astar.py`、`algorithm/benchmarks/benchmark_replan_10k.py`、`algorithm/api_server.py`、`algorithm/tests/test_path_hazard_avoidance.py`、`tools/code_quality_audit.py` |
| `algorithm/` 根目录继续混放 API 回归测试 | 新建 `algorithm/tests/`，将 API HTTP 语义测试迁入统一测试层，并补充错误响应 trace 字段断言；质量审计禁止 `algorithm/test_api_server_http_semantics.py` 回到根目录 | `algorithm/tests/test_api_server_http_semantics.py`、`algorithm/tests/__init__.py`、`tools/code_quality_audit.py` |
| `algorithm/` 根目录继续混放疏散路径回归测试 | 将路径危险规避测试迁入 `algorithm/tests/`，并进一步改为直接导入 `algorithm.planning` 子包实现；质量审计禁止 `algorithm/test_path_hazard_avoidance.py` 回到根目录或测试回退到包根兼容壳 | `algorithm/tests/test_path_hazard_avoidance.py`、`algorithm/README.md`、`docs/algorithm-verification-report.md`、`tools/code_quality_audit.py` |
| `algorithm/` 根目录继续混放解析溯源验证脚本 | 将解析溯源验证迁入 `algorithm/tests/`；脚本本身已使用绝对导入，迁移后仍以 3 个合成场景验证 coarse search + two-stage inversion，并由质量审计禁止 `algorithm/test_analytic_inversion.py` 回到根目录 | `algorithm/tests/test_analytic_inversion.py`、`tools/code_quality_audit.py` |
| `algorithm/` 根目录继续混放性能基准脚本 | 新建 `algorithm/benchmarks/`，将 10k/30k 重规划 benchmark 迁入基准目录并改为绝对导入；质量审计禁止 `algorithm/benchmark_replan_10k.py` 回到根目录 | `algorithm/benchmarks/benchmark_replan_10k.py`、`algorithm/benchmarks/__init__.py`、`algorithm/PATH_GAS_CONSTRAINTS.md`、`tools/code_quality_audit.py` |
| `algorithm/` 根目录继续承载旧扩散 + A* 疏散实现 monolith | 完整实现保留在 `algorithm/planning/gas_diffusion_astar.py` 作为非公开回归对象，包根 `algorithm/gas_diffusion_astar.py` 已删除；质量审计禁止重新恢复包根兼容导出 | `algorithm/planning/gas_diffusion_astar.py`、`algorithm/README.md`、`tools/code_quality_audit.py` |
| `algorithm/gas_diffusion_astar.py` 兼容壳仍用 `import *` 模糊包根边界 | 包根兼容壳已删除，不再保留旧 import path；质量审计禁止该文件回流 | `algorithm/README.md`、`tools/code_quality_audit.py` |
| `algorithm.gas_diffusion_astar` 兼容壳策略仍不够显式，内部代码可能重新依赖包根旧路径 | `algorithm.gas_diffusion_astar` 不再是有效入口；质量审计禁止整个 `algorithm/` 内部引用该包根旧路径 | `algorithm/README.md`、`tools/code_quality_audit.py` |
| `planning/gas_diffusion_astar.py` 继续同时承载气体目录、厂区布局、扩散和路径规划 | 把气体类型/物性表抽到 `gas_catalog.py`，把厂区建筑、道路图、出口和边键工具抽到 `factory_layout.py`，把 A* 路径搜索抽到 `astar_path_planner.py`，把旧深度代理扩散模型抽到 `legacy_diffusion_model.py`，把多源扩散/危险路网/建筑疏散编排抽到 `integrated_escape_system.py`；旧规划模块只通过包内导入复用，兼容导出不变；质量审计禁止气体目录、厂区布局、路径搜索、扩散模型和编排实现回填巨型文件 | `algorithm/planning/gas_catalog.py`、`algorithm/planning/factory_layout.py`、`algorithm/planning/astar_path_planner.py`、`algorithm/planning/legacy_diffusion_model.py`、`algorithm/planning/integrated_escape_system.py`、`algorithm/planning/gas_diffusion_astar.py`、`tools/code_quality_audit.py` |
| 内部旧扩散 + A* 回归对象是否继续保留的决策不明确，容易被误当新链入口 | 明确保留旧链作为非公开回归/兼容对象，不再删除有测试价值的历史实现；`gas_diffusion_astar.py`、`integrated_escape_system.py`、`legacy_diffusion_model.py` 均写入 `LEGACY_REGRESSION_ONLY = True`、`PUBLIC_SERVICE_EXPOSED = False` 和正式替代入口常量，README 与质量审计同步锁定 | `algorithm/planning/gas_diffusion_astar.py`、`algorithm/planning/integrated_escape_system.py`、`algorithm/planning/legacy_diffusion_model.py`、`algorithm/README.md`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 巨型文件承担 UI 事件与错误解析细节 | 先抽离 UI 事件值读取和错误响应解析到 `useSmartMapUi.ts`，并用质量审计防止这类辅助函数回填到巨型页面 | `frontend/src/views/smart_map/index.vue`、`frontend/src/views/smart_map/useSmartMapUi.ts`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续直接承担传感器/气体持久化和 `/sensor/*`、`/gas/*` 请求边界 | 新增 typed `sensor.ts` / `gas.ts` API 层，并把传感器/气体加载、保存、更新、删除抽到 `useSmartMapCatalogPersistence.ts`；`smart_map/index.vue` 编辑器口径降至 7314 行，质量审计禁止请求路径写回页面或 raw 行数重新超过 7500 | `frontend/src/api/sensor.ts`、`frontend/src/api/gas.ts`、`frontend/src/views/smart_map/useSmartMapCatalogPersistence.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联小车巡检状态、移动传感器采样、巡逻路径推进、车辆 Canvas 绘制和命中测试 | 新增 `useSmartMapCarPatrol.ts`，把小车标记同步、巡逻开关、DB 轮询保护、移动传感器读数、动画推进、车辆绘制和命中测试抽出；`smart_map/index.vue` 按审计脚本口径从 7482 行降至 7293 行，质量审计阈值收紧到 7300 并禁止巡检/绘制/命中测试实现回填 | `frontend/src/views/smart_map/useSmartMapCarPatrol.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联传感器当前浓度、自动采样浓度、告警等级和历史曲线构造 | 新增 `useSmartMapSensorSeries.ts`，把传感器序列读数和历史图计算移出页面；`smart_map/index.vue` 降至 7277 行，质量审计阈值继续收紧到 7280 并禁止历史图实现回填 | `frontend/src/views/smart_map/useSmartMapSensorSeries.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联传感器设备卡片、设备图片缓存、全屏缩放和拖拽 UI 状态 | 新增 `useSmartMapDeviceImage.ts`，把设备卡片构造、图片缓存和查看器状态机移出页面；`smart_map/index.vue` 降至 7205 行，质量审计阈值继续收紧到 7210 并禁止设备图控制实现回填 | `frontend/src/views/smart_map/useSmartMapDeviceImage.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联气体编辑面板 draft、保存和删除流程 | 新增 `useSmartMapGasEditor.ts`，把气体编辑弹窗状态、draft 赋值、删除确认、新增/更新保存和 toast 反馈移出页面；`smart_map/index.vue` 降至 4895 行，质量审计阈值收紧到 4900 并禁止 gas editor 函数回填 | `frontend/src/views/smart_map/useSmartMapGasEditor.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联 toast 状态、图标和隐藏定时器 | 新增 `useSmartMapToast.ts`，把 toast 可见状态、文案、类型、图标映射、展示函数和 timer 清理移出页面；`smart_map/index.vue` 降至 4890 行，质量审计阈值收紧到 4895 并禁止 toast 函数/定时器回填 | `frontend/src/views/smart_map/useSmartMapToast.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联筛选、图层开关、搜索、缩放和工具按钮编排 | 新增 `useSmartMapViewControls.ts`，把设施筛选、区域选择、出入口/传感器/半径/热力图/标签开关、搜索定位、地图缩放和工具切换移出页面；`smart_map/index.vue` 降至 4838 行，质量审计阈值收紧到 4845 并禁止这些控制函数回填 | `frontend/src/views/smart_map/useSmartMapViewControls.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联溯源精修播放状态、拖动定位步和动画推进 | 新增 `useSmartMapRefinementPlayback.ts`，把 `refinementState`、当前迭代、播放/暂停、step seek、重置和按帧推进逻辑移出页面；`smart_map/index.vue` 降至 4811 行，质量审计阈值收紧到 4815 并禁止 refinement 播放状态机回填 | `frontend/src/views/smart_map/useSmartMapRefinementPlayback.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联算法执行器状态和溯源/粒子滤波默认配置 | 新增 `useSmartMapAlgorithmStates.ts`，把扩散/疏散/溯源执行状态、粗定位配置、解析精修配置和粒子滤波配置移出页面；`smart_map/index.vue` 降至 4778 行，质量审计阈值收紧到 4785 并禁止这些 `reactive` 状态块回填 | `frontend/src/views/smart_map/useSmartMapAlgorithmStates.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联扩散场景表单、帧列表、meta 和气体选项状态 | 新增 `useSmartMapDiffusionScenario.ts`，把扩散场景默认值、可选泄漏源、播放速度、扩散帧、扩散 meta 和当前气体 computed 移出页面；`smart_map/index.vue` 降至 4766 行，质量审计阈值收紧到 4770 并禁止这些场景状态回填 | `frontend/src/views/smart_map/useSmartMapDiffusionScenario.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联传感器详情面板行构造 | 新增 `useSmartMapSensorInfo.ts`，把传感器标题、副标题、安装高度、范围、浓度、峰值、风险和经纬海拔行构造移出页面；`smart_map/index.vue` 降至 4748 行，质量审计阈值收紧到 4755 并禁止面板行构造回填 | `frontend/src/views/smart_map/useSmartMapSensorInfo.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联时钟和鼠标坐标展示状态 | 新增 `useSmartMapRuntimeDisplay.ts`，把右上角时钟、经纬度/海拔显示和更新函数移出页面；`smart_map/index.vue` 降至 4743 行，质量审计阈值收紧到 4750 并禁止显示状态回填 | `frontend/src/views/smart_map/useSmartMapRuntimeDisplay.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联气象展示状态和模拟初始化逻辑 | 新增 `useSmartMapWeatherState.ts`，把气象面板状态和 `initializeWeatherData()` 移出页面；默认仍为 `simulated`，不引入第三方天气 API、不宣称真实天气链路；`smart_map/index.vue` 降至 4734 行，质量审计阈值收紧到 4740 | `frontend/src/views/smart_map/useSmartMapWeatherState.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联右侧信息面板状态 | 新增 `useSmartMapInfoPanel.ts`，把选中/悬浮设施、入口、传感器以及标题、副标题和信息行状态移出页面；设施信息构造由信息面板 composable 统一调用，`smart_map/index.vue` 降至 4729 行，质量审计阈值收紧到 4735 | `frontend/src/views/smart_map/useSmartMapInfoPanel.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联传感器悬浮卡片 view model | 新增 `useSmartMapSensorHoverCard.ts`，把悬浮卡片中的传感器类型、当前浓度、峰值、风险等级和坐标标签计算移出页面；`smart_map/index.vue` 降至 4716 行，质量审计阈值收紧到 4720 并禁止 hover card computed 回填 | `frontend/src/views/smart_map/useSmartMapSensorHoverCard.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联小车详情面板行构造 | 新增 `useSmartMapCarInfo.ts`，把小车标题、状态、气体类型、阈值、详情跳转、设警和 YOLO 巡检行构造移出页面；`smart_map/index.vue` 降至 4705 行，质量审计阈值收紧到 4710 并禁止小车面板行构造回填 | `frontend/src/views/smart_map/useSmartMapCarInfo.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联小车选择、设警和 YOLO 检测动作 | 新增 `useSmartMapCarInteraction.ts`，把 `selectedCar/hoveredCar/yoloResult`、小车选择、YOLO 截图上传、结果同步和预警状态切换移出页面；`smart_map/index.vue` 降至 4532 行，质量审计阈值收紧到 4540 并禁止这些交互动作回填 | `frontend/src/views/smart_map/useSmartMapCarInteraction.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联覆盖率和风险等级统计状态 | 新增 `useSmartMapRiskSummary.ts`，把 `layoutResult`、`riskStat`、覆盖率计算和风险等级统计更新移出页面；本次是边界拆分，`smart_map/index.vue` 维持 4705 行，质量审计继续禁止这些状态和函数回填 | `frontend/src/views/smart_map/useSmartMapRiskSummary.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联溯源 workflow 状态和结果派生 computed | 新增 `useSmartMapSourceWorkflowState.ts`，把观测 payload、粗定位结果、候选区选择、精修输入/结果/摘要和粒子滤波迭代派生状态移出页面；`smart_map/index.vue` 降至 4687 行，质量审计阈值收紧到 4690 并禁止这些 ref/computed 回填 | `frontend/src/views/smart_map/useSmartMapSourceWorkflowState.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联观测 payload 写入、整理提示和 JSON 导出动作 | 新增 `useSmartMapObservationPayloadActions.ts`，把 `setObservationPayloadState`、`prepareObservationDataset`、导出按钮动作和真实性边界提示移出页面；`smart_map/index.vue` 降至 4483 行，质量审计阈值收紧到 4490 并禁止这些动作回填 | `frontend/src/views/smart_map/useSmartMapObservationPayloadActions.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联后端读数状态、扩散摘要和采样统计 computed | 新增 `useSmartMapMonitoringSummaryState.ts`，把读数匹配文案、真实性边界文案、扩散源/帧/模型/条件摘要和传感器采样统计移出页面；`smart_map/index.vue` 降至 4644 行，质量审计阈值收紧到 4650 并禁止这些 computed 回填 | `frontend/src/views/smart_map/useSmartMapMonitoringSummaryState.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 仍直接持有选中传感器曲线和观测 readiness computed | 新增 `useSmartMapSelectionDisplayState.ts`，把选中传感器历史曲线和溯源观测 readiness 摘要移出页面；`smart_map/index.vue` 当前 4648 行，质量审计禁止 `computed(` 回填页面 | `frontend/src/views/smart_map/useSmartMapSelectionDisplayState.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联前端轻量物理浓度估算 | 新增 `smartMapLightweightConcentration.ts`，把占位传感器读数的轻量估算和点位归一化移出页面，并保留“优先使用后端扩散/后端读数”的真实性边界；`smart_map/index.vue` 降至 4602 行，质量审计阈值收紧到 4610 并禁止估算实现回填 | `frontend/src/views/smart_map/smartMapLightweightConcentration.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联传感器高度、半径、检测范围和备注解析 | 新增 `smartMapSensorDimensions.ts`，把传感器尺寸/范围 resolver 移出页面并复用手动布点默认值归一化；`smart_map/index.vue` 降至 4511 行，质量审计阈值收紧到 4520 并禁止 resolver 实现回填 | `frontend/src/views/smart_map/smartMapSensorDimensions.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联传感器聚焦视图定位计算 | 新增 `smartMapSensorFocus.ts`，把 `zoomToSensor` 的缩放和 offset 计算移出页面；`smart_map/index.vue` 降至 4481 行，质量审计阈值收紧到 4485 并禁止该计算回填 | `frontend/src/views/smart_map/smartMapSensorFocus.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联撤离规划动作编排 | 新增 `useSmartMapEvacuationPlanningActions.ts`，把起点解析、单建筑/批量规划、候选路线/建筑选择、清理和设施切换同步移出页面；`smart_map/index.vue` 降至 4354 行，质量审计阈值收紧到 4360 并禁止这些动作函数回填 | `frontend/src/views/smart_map/useSmartMapEvacuationPlanningActions.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联传感器序列依赖拼装 | 新增 `useSmartMapSensorSeriesActions.ts`，把当前浓度/自动浓度/告警等级包装、手工序列归一化、活动传感器序列重采样和 demo 清空动作移出页面；`smart_map/index.vue` 降至 4299 行，质量审计阈值收紧到 4305 并禁止这些包装函数回填 | `frontend/src/views/smart_map/useSmartMapSensorSeriesActions.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联观测 payload 和粒子滤波 payload 构造 | 新增 `useSmartMapObservationBuilders.ts`，把观测传感器筛选、payload 构造、观测摘要、真实/仿真读数刷新、粒子滤波边界和 payload 构造移出页面；`smart_map/index.vue` 降至 4229 行，质量审计阈值收紧到 4235 并禁止这些构造函数回填 | `frontend/src/views/smart_map/useSmartMapObservationBuilders.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联溯源流程动作编排 | 新增 `useSmartMapSourceInversionActions.ts`，把粗搜、候选区选择、解析精修、粒子滤波精定位和溯源结果清理移出页面；`smart_map/index.vue` 降至 4066 行，质量审计阈值收紧到 4075 并禁止这些流程函数回填 | `frontend/src/views/smart_map/useSmartMapSourceInversionActions.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 仍包含大段 scoped 样式，导致文件继续超过 4000 行 | 将 `<style scoped>` 机械迁移到 `index.css`，页面改为 `<style scoped src="./index.css"></style>`；`smart_map/index.vue` 降至 2693 行，质量审计阈值收紧到 2700 并要求外置样式文件存在 | `frontend/src/views/smart_map/index.css`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联 BTEX / Prairie Grass 真实验证报告加载和展示摘要 | 新增 `useSmartMapValidationReports.ts`，把真实验证报告 API 加载、状态 class、样本/源强摘要、定位误差和浓度形状摘要移出页面；`smart_map/index.vue` 降至 7098 行，质量审计阈值继续收紧到 7100 并禁止验证报告 API/摘要逻辑回填 | `frontend/src/views/smart_map/useSmartMapValidationReports.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 观测 payload 仍容易混淆仿真、手工录入、移动采样和真实 `sensor_reading` | `useSmartMapInversion.ts` 为每个观测点写入 `observationSource`，为整包写入 `observationSourceSummary/formalValidationAllowed/warnings`；`smart_map` 面板显示“读数来源/真实性边界”，没有真实 `sensor_reading` 时明确提示“仿真/手工，不可作为真实验证” | `frontend/src/views/smart_map/useSmartMapInversion.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 后端已有 `sensor_reading` 但 `smart_map` 未纳入正式观测链，且容易把仿真读数当实测 | 新增 typed `simulationMonitoring.ts` 和 `useSmartMapSensorReadings.ts`，从 `/api/simulation-monitoring/readings/recent` 拉取读数并按 sensorId 合并进溯源观测；`source=simulation` 或 `qualityStatus=SIMULATED` 统一标为 `sensor_reading_simulated`，不允许触发 `formalValidationAllowed` | `frontend/src/api/simulationMonitoring.ts`、`frontend/src/views/smart_map/useSmartMapSensorReadings.ts`、`frontend/src/views/smart_map/useSmartMapInversion.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 项目没有真实硬件数据，仍可能被二次改动误写成真实验证链路 | 前端读数合并侧新增 `SMART_MAP_MONITORING_DATA_TRUTH_MODE = 'simulation_only'`，观测 payload 侧新增 `SMART_MAP_FORMAL_VALIDATION_POLICY = 'requires_trusted_sensor_reading'`；质量审计要求这两个策略常量存在，继续把当前仓库读数限定为仿真演示，不允许冒充正式验证 | `frontend/src/views/smart_map/useSmartMapSensorReadings.ts`、`frontend/src/views/smart_map/useSmartMapInversion.ts`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联传感器布点几何判断和 GB 风险计算规则 | 新增 `useSmartMapSensorPlacementRules.ts`，把设施锚点/跨度、点位邻近判断、风险等级标签/颜色、GB 18218 R 值风险计算和最近设施查找移出页面；`smart_map/index.vue` 降至 6932 行，质量审计阈值收紧到 6935 并禁止规则实现回填 | `frontend/src/views/smart_map/useSmartMapSensorPlacementRules.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续直接调用扩散、粗定位、精定位和疏散算法 API | 新增 `useSmartMapAlgorithmExecutors.ts`，把算法 API 调用、响应拆包和必要结果校验移出页面；页面只保留状态流转、渲染和提示，质量审计禁止 `@/api/algorithm` 重新回填到 `smart_map/index.vue` | `frontend/src/views/smart_map/useSmartMapAlgorithmExecutors.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联疏散规划状态、候选路线选择和摘要计算 | 新增 `useSmartMapEvacuationPlanning.ts`，把单建筑/全建筑疏散结果、候选路线、选中建筑、推荐路线和摘要文本移出页面；`smart_map/index.vue` 降至 6907 行，质量审计阈值收紧到 6910 并禁止疏散状态 computed 回填 | `frontend/src/views/smart_map/useSmartMapEvacuationPlanning.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联传感器编辑和手动采样序列状态机 | 新增 `useSmartMapSensorEditor.ts`，把传感器参数编辑、手动序列写入/填充/复制/清空、手动面板目标选择移出页面；`smart_map/index.vue` 降至 6726 行，质量审计阈值收紧到 6730 并禁止编辑状态机回填 | `frontend/src/views/smart_map/useSmartMapSensorEditor.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联手动传感器布点、零点选取和删除/清空状态机 | 新增 `useSmartMapSensorPlacement.ts`，把手动布点草稿、零点/相对坐标、候选点确认/取消、清空全部和删除当前传感器移出页面；主页面只保留传感器编号、风险计算和保存业务；`smart_map/index.vue` 降至 6566 行，质量审计阈值收紧到 6570 | `frontend/src/views/smart_map/useSmartMapSensorPlacement.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联批量导入传感器解析和保存循环 | 新增 `useSmartMapSensorBatchImport.ts`，把 Excel 粘贴解析、预览列表、默认高度/范围、逐条保存和刷新 DB 列表移出页面；`smart_map/index.vue` 降至 6505 行，质量审计阈值收紧到 6510 | `frontend/src/views/smart_map/useSmartMapSensorBatchImport.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联画布视图坐标、缩放和边界夹紧 | 新增 `useSmartMapViewport.ts`，把 `viewState`、世界/屏幕坐标转换、初始适配、滚轮/按钮缩放、边界夹紧和焦点定位移出页面；`smart_map/index.vue` 降至 6440 行，质量审计阈值收紧到 6445 并禁止视图函数回填 | `frontend/src/views/smart_map/useSmartMapViewport.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联设施、入口、候选区和传感器命中测试 | 新增 `useSmartMapHitTesting.ts`，把筛选匹配、可见入口合并、设施/入口/候选区/点位命中测试移出页面；`smart_map/index.vue` 降至 6394 行，质量审计阈值收紧到 6400 并禁止命中测试函数回填 | `frontend/src/views/smart_map/useSmartMapHitTesting.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联设施详情面板行构造 | 新增 `useSmartMapFacilityInfo.ts`，把设施状态标签、区域名解析和储罐/塔器/建筑详情行构造移出页面；`smart_map/index.vue` 降至 6355 行，质量审计阈值收紧到 6360 并禁止面板行构造回填 | `frontend/src/views/smart_map/useSmartMapFacilityInfo.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联测距工具状态和 Canvas 绘制 | 新增 `useSmartMapMeasureTool.ts`，把 `measureMode`、测距点数组、工具切换、测距绘制和光标状态移出页面；`smart_map/index.vue` 降至 6331 行，质量审计阈值收紧到 6335 并禁止 `measurePoints/drawMeasure` 回填 | `frontend/src/views/smart_map/useSmartMapMeasureTool.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联 Canvas 鼠标拖拽、悬停和点击选择分发 | 新增 `useSmartMapCanvasInteraction.ts`，把 `onCanvasMouseDown/Move/Up/Leave/Wheel`、拖拽、悬停、测距点拾取、泄漏源/传感器拾取和设施/传感器/小车/候选区选择分发移出页面；`smart_map/index.vue` 降至 6261 行，质量审计阈值收紧到 6265 并禁止鼠标函数回填 | `frontend/src/views/smart_map/useSmartMapCanvasInteraction.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联扩散帧 Canvas 绘制，且边界/骨架/羽流结构复用 `AnyRecord` | 新增 `useSmartMapDiffusionLayer.ts`，把扩散网格、边界多边形、骨架线和羽流椭圆绘制抽出，并为 `cells/boundaryPolygons/contourSkeletons/plume` 补明确类型；`smart_map/index.vue` 降至 6205 行，质量审计阈值收紧到 6210 并禁止扩散边界/骨架绘制回填 | `frontend/src/views/smart_map/useSmartMapDiffusionLayer.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联溯源候选区、精定位轮廓和预测源点 Canvas 绘制 | 新增 `useSmartMapSourceInversionOverlay.ts`，把粗定位候选区、R 轮廓和预测源点图标绘制抽出，并把 `coarseCandidateRegions/refinementIterations` 从 `AnyRecord[]` 收窄为显式候选区与迭代结构；`smart_map/index.vue` 降至 6117 行，质量审计阈值收紧到 6120 并禁止溯源叠加绘制回填 | `frontend/src/views/smart_map/useSmartMapSourceInversionOverlay.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联疏散路径 Canvas 绘制并把路线入口声明为 `AnyRecord` | 新增 `useSmartMapEvacuationRouteCanvas.ts`，把单路线/全建筑路线绘制和起终点标记抽出，并在 `SmartMapEvacuationRoute` 上声明 `path` 结构；`smart_map/index.vue` 降至 6056 行，质量审计阈值收紧到 6060 并禁止疏散路线绘制回填 | `frontend/src/views/smart_map/useSmartMapEvacuationRouteCanvas.ts`、`frontend/src/views/smart_map/useSmartMapEvacuationPlanning.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联设施 bounds、泄漏源 marker、设施选中/悬停 Canvas 叠加 | 新增 `useSmartMapFacilityCanvas.ts`，把设施 bounds、半径设施类型守卫、泄漏源 marker、选中框和悬停框绘制抽出；主页面仍复用同一 bounds 函数给命中测试，`smart_map/index.vue` 降至 5996 行，质量审计阈值收紧到 6000 并禁止这些叠加绘制回填 | `frontend/src/views/smart_map/useSmartMapFacilityCanvas.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联入口 marker、连接线、tooltip 和圆角框绘制 | 新增 `useSmartMapEntranceCanvas.ts`，把园区/建筑入口绘制与悬停 tooltip 抽出，主页面只传可见入口、悬停入口和地图尺寸；`smart_map/index.vue` 降至 5902 行，质量审计阈值收紧到 5905 并禁止入口绘制回填 | `frontend/src/views/smart_map/useSmartMapEntranceCanvas.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联传感器 Canvas 点位、范围圈和选中高亮绘制 | 新增 `useSmartMapSensorCanvas.ts`，把传感器点位、风险颜色范围圈和选中高亮抽出；主页面只传显示开关、传感器列表、类型列表、范围解析和优先级颜色函数；`smart_map/index.vue` 降至 5875 行，质量审计阈值收紧到 5880 并禁止点位绘制细节回填 | `frontend/src/views/smart_map/useSmartMapSensorCanvas.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联活动传感器序列构造、无扩散帧确定性仿真采样和后端读数合并 | 扩展 `useSmartMapSensorSeries.ts`，把 `buildSmartMapActiveSensorSeries()`、扩散帧采样、手动序列归一化、轻量物理响应回退和 `sensor_reading` 合并移出页面；主页面仅注入泄漏点、气象、风险计算和后端读数，并把序列包装函数收窄到 `SmartMapSeriesPoint` / `SmartMapRecord[]`；`smart_map/index.vue` 按审计脚本口径降至 5787 行，质量审计阈值收紧到 5790 并禁止序列构造实现回填 | `frontend/src/views/smart_map/useSmartMapSensorSeries.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联风险网格计算、覆盖率统计和热力图绘制 | 新增 `useSmartMapRiskGrid.ts`，把 GB 思想风险网格、下风向修正、动态最小间距、覆盖率统计、风险等级汇总和热力图绘制移出页面；主页面仅保存结果 ref 并复用配置生成建议点；`smart_map/index.vue` 按审计脚本口径降至 5614 行，质量审计阈值收紧到 5620 并禁止风险网格实现回填 | `frontend/src/views/smart_map/useSmartMapRiskGrid.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联标准传感器布局、工程编号生成和手动新增传感器保存编排 | 新增 `useSmartMapSensorLayout.ts`，把 canonical 布点回退、GB 思想建议点筛选、动态最小间距复用和 TK/TW/PF/SC 等编号生成移出页面；手动新增传感器的编号、风险、序列模板、选中、保存和刷新编排继续下沉到 `useSmartMapSensorPlacement.ts`；`smart_map/index.vue` 按审计脚本口径降至 5457 行，质量审计阈值收紧到 5460 并禁止布局生成和手动新增实现回填 | `frontend/src/views/smart_map/useSmartMapSensorLayout.ts`、`frontend/src/views/smart_map/useSmartMapSensorPlacement.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联基础 Canvas 底图、重点区、标签和热力图绘制 | 新增 `useSmartMapBaseCanvas.ts`，把底图、道路、重点区、管线、建筑、储罐、塔器、标签和基础热力图绘制函数移出页面；主页面只保留实际渲染调用顺序；`smart_map/index.vue` 降至 5257 行，质量审计阈值收紧到 5260 并禁止基础绘图函数回填 | `frontend/src/views/smart_map/useSmartMapBaseCanvas.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联泄漏源选择、经纬度输入、合法性校验和源点 watch | 新增 `useSmartMapLeakSource.ts`，把泄漏源状态、设施/地图/经纬点选、合法源校验、扩散 meta 同步、气体切换和源设施切换 watch 移出页面；`smart_map/index.vue` 降至 5097 行，质量审计阈值收紧到 5100 并禁止泄漏源选择实现回填 | `frontend/src/views/smart_map/useSmartMapLeakSource.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联扩散/溯源流程进度 computed | 新增 `useSmartMapWorkflowSteps.ts`，把命令流程和溯源流程 step 状态从页面抽出；`smart_map/index.vue` 降至 5068 行，质量审计阈值收紧到 5070 并禁止 workflow computed 回填 | `frontend/src/views/smart_map/useSmartMapWorkflowSteps.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联扩散播放状态、当前帧 computed 和帧推进函数 | 新增 `useSmartMapDiffusionPlayback.ts`，把扩散播放状态、当前帧、running 标记、重置、首帧播放、seek、step 和按时间推进逻辑移出页面；`smart_map/index.vue` 降至 5036 行，质量审计阈值收紧到 5040 并禁止播放函数回填 | `frontend/src/views/smart_map/useSmartMapDiffusionPlayback.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联扩散算法调用、payload 构造、结果 meta 和重置编排 | 新增 `useSmartMapDiffusionSimulation.ts`，把扩散 payload 构造、算法服务调用、结果帧/meta 写入、扩散后逃生路径刷新和 reset 流程移出页面；`smart_map/index.vue` 降至 4950 行，质量审计阈值收紧到 4955 并禁止扩散运行函数回填 | `frontend/src/views/smart_map/useSmartMapDiffusionSimulation.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 仍在页面内维护入口/小车/扩散源/风险网格/传感器等图层包装函数 | 新增 `useSmartMapCanvasLayerRenderers.ts`，把这些图层级渲染适配统一移出页面；主页面只保留渲染顺序和状态注入；`smart_map/index.vue` 降至 2684 行，质量审计阈值收紧到 2690 并禁止底层绘制函数直接回填页面 | `frontend/src/views/smart_map/useSmartMapCanvasLayerRenderers.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联 Canvas 生命周期、resize、动画帧和时钟定时器 | 新增 `useSmartMapCanvasRuntime.ts`，把 Canvas/context 绑定、resize、2D/3D 切换重绑、动画帧、时钟定时器和卸载清理移出页面；业务初始化仍留在页面显式注入，避免 runtime 变成业务容器；`smart_map/index.vue` 降至 2638 行，质量审计阈值收紧到 2645 并禁止生命周期调度回填 | `frontend/src/views/smart_map/useSmartMapCanvasRuntime.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联业务启动清单、动画帧业务副作用和 watcher 清理编排 | 新增 `useSmartMapLifecycleCoordinator.ts`，把 Canvas ready 后源点初始化、扩散/精修/巡检帧推进、传感器/小车同步、设施疏散同步、扩散输入变化后的溯源/疏散清理和车载刷新定时器移出页面；`smart_map/index.vue` 降至 2611 行，质量审计阈值收紧到 2615 并禁止 watcher/业务 runtime 清单回填 | `frontend/src/views/smart_map/useSmartMapLifecycleCoordinator.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联主 Canvas 渲染顺序、边界裁剪和图层拼装 | 新增 `useSmartMapRenderer.ts`，把底图、扩散层、溯源叠加、疏散路线、风险网格、传感器、小车、入口和测距图层的渲染编排移出页面；页面只保留 `render()` 入口和状态 getter 注入；`smart_map/index.vue` 降至 2556 行，质量审计阈值收紧到 2560 并禁止主渲染顺序回填 | `frontend/src/views/smart_map/useSmartMapRenderer.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联传感器聚焦、关闭面板、返回首页和条件扩散 demo 装填 | 新增 `useSmartMapPageActions.ts`，把后置模板动作、传感器聚焦和条件扩散 demo 参数装填移出页面；早期注入依赖的 `navigateToCarDetail/showFacilityInfo/clearInfo` 暂保留为本地胶水；`smart_map/index.vue` 降至 2540 行，质量审计阈值收紧到 2545 并禁止 demo/聚焦动作回填 | `frontend/src/views/smart_map/useSmartMapPageActions.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联风险网格计算 wrapper | 扩展 `useSmartMapRiskGrid.ts`，新增 `useSmartMapRiskGridActions()`，把天气读取、网格计算、ref 写回移出页面；`smart_map/index.vue` 降至 2536 行，质量审计阈值收紧到 2540 并禁止 wrapper 回填 | `frontend/src/views/smart_map/useSmartMapRiskGrid.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联传感器详情面板动作体 | 扩展 `useSmartMapSensorInfo.ts`，新增 `useSmartMapSensorInfoActions()`，把右侧面板展开、手动传感器目标同步、详情行构造和编辑状态同步移出页面；页面保留 hoisted 薄 wrapper，避免早期 composable 注入引用出现初始化顺序风险；`smart_map/index.vue` 降至 2535 行，质量审计阈值收紧到 2535 并禁止详情构造体回填 | `frontend/src/views/smart_map/useSmartMapSensorInfo.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续保留本地导航和信息面板清理函数 | 扩展 `useSmartMapPageActions.ts`，新增 `navigateToSmartMapCarDetail()` 与 `clearSmartMapInfo()`，让小车详情跳转、右侧面板清空和已抽出的 page actions 共用同一实现；页面移除 `navigateToCarDetail/showFacilityInfo/clearInfo` 本地函数，`smart_map/index.vue` 降至 2527 行，质量审计阈值收紧到 2527 并禁止三段函数回填 | `frontend/src/views/smart_map/useSmartMapPageActions.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联 Canvas 选择分发动作 | 新增 `useSmartMapCanvasSelectionActions.ts`，把传感器、候选区、设施和清空选择的状态写入动作移出页面；主页面只向 `useSmartMapCanvasInteraction` 注入已组装的 action；`smart_map/index.vue` 降至 2524 行，质量审计阈值收紧到 2524 并禁止内联选择分发回填 | `frontend/src/views/smart_map/useSmartMapCanvasSelectionActions.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联 hit-test 状态读取 wrapper | 扩展 `useSmartMapHitTesting.ts`，新增 `useSmartMapHitTestingActions()`，把入口可见性、设施筛选、设施/入口/候选区/传感器命中测试对当前状态的读取移出页面；`smart_map/index.vue` 降至 2523 行，质量审计阈值收紧到 2523 并禁止本地 hit-test wrapper 回填 | `frontend/src/views/smart_map/useSmartMapHitTesting.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续保留最近设施查找本地 wrapper | 移除 `findNearestFacility()` 本地函数，各调用点直接注入 `findNearestSmartMapFacility(x, y, facilities)`，避免页面继续保留无业务含义的薄包装；`smart_map/index.vue` 降至 2519 行，质量审计阈值收紧到 2519 并禁止该 wrapper 回填 | `frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续直接持有 Canvas/context/render 胶水 | 新增 `useSmartMapRenderBridge.ts`，把 `canvasEl`、`ctx`、renderer 绑定、光标设置和 render 调度桥接移出页面；页面只保留 `getCanvas/render/setCanvasCursor` 注入，`smart_map/index.vue` 降至 2517 行，质量审计阈值收紧到 2517 并禁止 `ctx/canvasEl/function render()` 回填 | `frontend/src/views/smart_map/useSmartMapRenderBridge.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续保留 `showSensorInfo` 初始化顺序 wrapper | 扩展 `useSmartMapSensorInfo.ts`，新增 `useSmartMapSensorInfoActionBridge()`，用可延迟绑定的 action bridge 取代页面本地函数声明；`smart_map/index.vue` 降至 2515 行，质量审计阈值收紧到 2515 并禁止本地 wrapper 回填 | `frontend/src/views/smart_map/useSmartMapSensorInfo.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续直接初始化地图图片并绑定 onload 渲染 | 扩展 `useSmartMapRenderBridge.ts`，新增 `createRenderImage()`，把 `new Image()`、`src` 和 `onload render` 绑定移出页面；`smart_map/index.vue` 降至 2514 行，质量审计阈值收紧到 2514 并禁止图片加载胶水回填 | `frontend/src/views/smart_map/useSmartMapRenderBridge.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续直接持有 Canvas 外壳 refs | 新增 `useSmartMapCanvasShell.ts`，把 `mapCanvasRef`、`mapContainerRef`、`isDragging`、`viewMode` 和 `scene3DRef` 移出页面；`smart_map/index.vue` 降至 2511 行，质量审计阈值收紧到 2511 并禁止这些 refs 回填 | `frontend/src/views/smart_map/useSmartMapCanvasShell.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续直接持有传感器/气体/风险网格根状态 | 新增 `useSmartMapCoreState.ts`，把 `sensors`、`gases`、`riskGrid` 和 `selectedSensor` 这些共享根 ref 移出页面；`smart_map/index.vue` 降至 2509 行，质量审计阈值收紧到 2509 并禁止根状态 ref 回填 | `frontend/src/views/smart_map/useSmartMapCoreState.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续保留未使用的 route hook | 删除未使用的 `useRoute()` / `route` 声明，页面只保留实际用于导航的 `useRouter()`；`smart_map/index.vue` 降至 2508 行，质量审计阈值收紧到 2508 并禁止该死 hook 回填 | `frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 仍用 `as unknown` 抹平扩散帧和观测记录边界 | 将 `SmartMapDiffusionFrame` 明确声明为 `SmartMapRecord`，页面向小车巡检和传感器编辑注入当前帧时不再做双重类型转换；`useSmartMapSensorEditor` 的帧约束收窄为只依赖 `frameIndex`；`smart_map/index.vue` 降至 2507 行，质量审计阈值收紧到 2507 并禁止这两类 cast 回填 | `frontend/src/views/smart_map/useSmartMapDiffusionLayer.ts`、`frontend/src/views/smart_map/useSmartMapSensorEditor.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续保留 Canvas runtime 绑定匿名桥接 | 扩展 `useSmartMapRenderBridge.ts`，新增 `bindRuntimeCanvas()` 直接匹配 runtime 的 `onCanvasBound` 签名；页面改为传函数引用，不再内联 canvas/context 绑定回调；`smart_map/index.vue` 降至 2506 行，质量审计阈值收紧到 2506 并禁止匿名桥接回填 | `frontend/src/views/smart_map/useSmartMapRenderBridge.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `selectedDiffusionSource` 仍由监测摘要模块临时派生，页面多处重复注入泄漏源/扩散源 getter | 将当前泄漏源设施归属到 `useSmartMapDiffusionScenario.ts`，并由其返回 `getSelectedDiffusionSource()`；`useSmartMapMonitoringSummaryState.ts` 改为消费该状态；`useSmartMapLeakSource.ts` 返回 `getCurrentLeakSourcePoint()`，页面提前初始化泄漏源状态并移除重复 `() => currentLeakSourcePoint.value` / `selectedDiffusionSource.value` 注入，同时删掉未使用的 `seedDemoSensors` 解构；质量审计禁止这些 getter 回填 | `frontend/src/views/smart_map/useSmartMapDiffusionScenario.ts`、`frontend/src/views/smart_map/useSmartMapMonitoringSummaryState.ts`、`frontend/src/views/smart_map/useSmartMapLeakSource.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续用自调用 wrapper 跨初始化顺序同步传感器编辑状态 | 扩展 `useSmartMapSensorEditor.ts`，新增 `useSmartMapSensorEditorSyncBridge()`；早期传感器序列动作注入 bridge，真实 editor 初始化后绑定实际 `syncSensorEditorState`；页面移除 `sensor => syncSensorEditorState(sensor)` wrapper，并清理两个未使用泄漏源解构项，行数保持 2506；质量审计禁止该 wrapper 回填 | `frontend/src/views/smart_map/useSmartMapSensorEditor.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续给气体编辑器传浏览器确认回调 | `useSmartMapGasEditor.ts` 内置 `confirmGasDelete()` 默认确认逻辑，页面删除 `confirmDelete: message => confirm(message)` 注入；`smart_map/index.vue` 降至 2505 行，质量审计阈值收紧到 2505 并禁止该 wrapper 回填 | `frontend/src/views/smart_map/useSmartMapGasEditor.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续直接写 `leakSourceState.picking = false` 关闭泄漏源点选 | `useSmartMapLeakSource.ts` 暴露 `cancelLeakSourcePicking()`，传感器布点模块直接注入该动作；页面删除跨模块状态写入，`smart_map/index.vue` 降至 2504 行，质量审计阈值收紧到 2504 并禁止该状态写回页面 | `frontend/src/views/smart_map/useSmartMapLeakSource.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 3D 园区组件点击设施时仍在模板内直接写 `selectedFacility` | 扩展 `useSmartMapCanvasSelectionActions.ts`，新增 `setSelectedFacilityById()`，`ParkScene3D` 的 `facility-click` 事件改为调用选择 action；页面不再内联 `selectedFacility = facilityById.get(id)`，质量审计禁止该写法回填 | `frontend/src/views/smart_map/useSmartMapCanvasSelectionActions.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 泄漏源模块仍通过页面闭包直接改 `sensorPlacementState.picking` / `pickingOrigin` | `useSmartMapSensorPlacement.ts` 暴露 `cancelSensorPicking()` / `cancelSensorOriginPicking()`，泄漏源模块只调用布点动作；页面删除跨模块状态写入，`smart_map/index.vue` 降至 2502 行，质量审计阈值收紧到 2502 并禁止该状态写回页面 | `frontend/src/views/smart_map/useSmartMapSensorPlacement.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 生命周期初始化仍在页面重复拼接扩散源点 | `useSmartMapLifecycleCoordinator` 的 `getInitialSourcePoint` 改为复用 `useSmartMapLeakSource.ts` 暴露的 `getCurrentLeakSourcePoint()`；质量审计禁止 `getFacilityAnchorPoint(getSelectedDiffusionSource())` 的初始化 wrapper 回填 | `frontend/src/views/smart_map/index.vue`、`frontend/src/views/smart_map/useSmartMapLeakSource.ts`、`tools/code_quality_audit.py` |
| renderer 仍在页面重复注入扩散源点、源名和显示条件 getter | `useSmartMapLeakSource.ts` 返回 `diffusionSourceLayer`，`useSmartMapRenderer.ts` 直接消费该 overlay 对象；页面删除三条 renderer inline getter，`smart_map/index.vue` 降至 2501 行，质量审计阈值收紧到 2501 并禁止旧 getter 回填 | `frontend/src/views/smart_map/useSmartMapLeakSource.ts`、`frontend/src/views/smart_map/useSmartMapRenderer.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 批量导入仍通过页面读取传感器布点零点状态 | `useSmartMapSensorPlacement.ts` 暴露 `getSensorPlacementOrigin()`，`useSmartMapSensorBatchImport.ts` 注入该 getter；质量审计禁止 `getOrigin: () => sensorPlacementState.origin` 回填 | `frontend/src/views/smart_map/useSmartMapSensorPlacement.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 扩散模拟完成后重跑疏散规划的编排仍留在页面闭包中 | `useSmartMapEvacuationPlanningActions.ts` 暴露 `rerunEvacuationAfterDiffusion()`，扩散模拟模块直接注入该 action；页面删除疏散重跑闭包，`smart_map/index.vue` 降至 2496 行，质量审计阈值收紧到 2496 并禁止旧闭包回填 | `frontend/src/views/smart_map/useSmartMapEvacuationPlanningActions.ts`、`frontend/src/views/smart_map/useSmartMapDiffusionSimulation.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| Canvas interaction 仍通过页面 wrapper 读取泄漏源/传感器点选状态 | `useSmartMapLeakSource.ts` 暴露 `isLeakSourcePicking()`，`useSmartMapSensorPlacement.ts` 暴露 `isSensorPicking()` / `isSensorOriginPicking()`，Canvas interaction 直接注入语义 getter；`smart_map/index.vue` 保持 2496 行，质量审计禁止旧 wrapper 回填 | `frontend/src/views/smart_map/useSmartMapLeakSource.ts`、`frontend/src/views/smart_map/useSmartMapSensorPlacement.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 扩散模拟重置仍通过页面 wrapper 静默清理疏散规划 | `useSmartMapEvacuationPlanningActions.ts` 暴露 `clearEvacuationPlanningSilently()`，扩散模拟模块直接注入静默清理 action；`smart_map/index.vue` 保持 2496 行，质量审计禁止 `clearEvacuationPlanning(true)` wrapper 回填 | `frontend/src/views/smart_map/useSmartMapEvacuationPlanningActions.ts`、`frontend/src/views/smart_map/useSmartMapDiffusionSimulation.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 多个模块仍通过页面 wrapper 读取当前扩散帧和帧序号 | `useSmartMapDiffusionPlayback.ts` 暴露 `getCurrentDiffusionFrame()` / `getCurrentDiffusionFrameIndex()`，小车巡检、传感器 hover、传感器编辑和 renderer 直接注入播放 getter；`smart_map/index.vue` 保持 2496 行，质量审计禁止旧 wrapper 回填 | `frontend/src/views/smart_map/useSmartMapDiffusionPlayback.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 传感器 hover 和 renderer 仍通过页面 wrapper 读取当前扩散气体 | `useSmartMapDiffusionScenario.ts` 暴露 `getCurrentDiffusionGas()`，传感器 hover 和 renderer 直接注入该 getter；`smart_map/index.vue` 保持 2496 行，质量审计禁止旧气体 wrapper 回填 | `frontend/src/views/smart_map/useSmartMapDiffusionScenario.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 传感器布点仍通过页面 wrapper 拼接信息面板清理参数 | `useSmartMapPageActions.ts` 新增 `createSmartMapInfoClearAction()`，页面给传感器布点注入可复用清理 action；`smart_map/index.vue` 保持 2496 行，质量审计禁止旧 `clearSmartMapInfo(...)` wrapper 回填 | `frontend/src/views/smart_map/useSmartMapPageActions.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 风险统计和 renderer 仍通过页面 wrapper 读取核心传感器/风险网格状态 | `useSmartMapCoreState.ts` 暴露 `getSensors()`、`getRiskGrid()`、`getSelectedSensorId()`，风险统计和 renderer 直接注入核心状态 getter；`smart_map/index.vue` 保持 2496 行，质量审计禁止旧核心状态 wrapper 回填 | `frontend/src/views/smart_map/useSmartMapCoreState.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| renderer 仍通过页面 wrapper 读取地图显示开关状态 | `useSmartMapViewControls.ts` 返回 `viewVisibility`，renderer 直接消费热力图、标签、传感器、传感器半径和出入口显示 getter；`smart_map/index.vue` 降至 2493 行，质量审计阈值收紧到 2493 并禁止旧显示 wrapper 回填 | `frontend/src/views/smart_map/useSmartMapViewControls.ts`、`frontend/src/views/smart_map/useSmartMapRenderer.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| renderer 仍通过页面 wrapper 读取小车图层状态 | `useSmartMapCarPatrol.ts` 返回 `carLayerState`，renderer 直接消费小车显示、marker 和移动传感器读数 getter；`smart_map/index.vue` 降至 2491 行，质量审计阈值收紧到 2491 并禁止旧小车图层 wrapper 回填 | `frontend/src/views/smart_map/useSmartMapCarPatrol.ts`、`frontend/src/views/smart_map/useSmartMapRenderer.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| renderer 仍通过页面 wrapper 读取测距绘制状态 | `useSmartMapMeasureTool.ts` 返回 `measureLayer`，renderer 直接消费测距点存在判断和绘制函数；`smart_map/index.vue` 降至 2489 行，质量审计阈值收紧到 2489 并禁止旧测距 wrapper 回填 | `frontend/src/views/smart_map/useSmartMapMeasureTool.ts`、`frontend/src/views/smart_map/useSmartMapRenderer.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| renderer 仍通过页面 wrapper 读取设施和入口图层状态 | `useSmartMapInfoPanel.ts` 返回 `facilityLayerState`，renderer 直接消费选中设施、hover 设施和 hover 入口 getter；`smart_map/index.vue` 降至 2488 行，质量审计阈值收紧到 2488 并禁止旧设施/入口 wrapper 回填 | `frontend/src/views/smart_map/useSmartMapInfoPanel.ts`、`frontend/src/views/smart_map/useSmartMapRenderer.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| renderer 仍通过页面 wrapper 读取车辆选中和 hover 状态 | `useSmartMapCarInteraction.ts` 返回 `carInteractionLayerState`，renderer 直接消费选中车辆和 hover 车辆 getter；`smart_map/index.vue` 保持 2488 行，质量审计禁止旧车辆状态 wrapper 回填 | `frontend/src/views/smart_map/useSmartMapCarInteraction.ts`、`frontend/src/views/smart_map/useSmartMapRenderer.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| renderer 仍通过页面 wrapper 读取疏散路线图层状态 | `useSmartMapEvacuationPlanning.ts` 返回 `evacuationLayerState`，renderer 直接消费规划模式、显示模式、当前路线、楼宇路线和选中楼宇路线 getter；`smart_map/index.vue` 降至 2485 行，质量审计阈值收紧到 2485 并禁止旧疏散图层 wrapper 回填 | `frontend/src/views/smart_map/useSmartMapEvacuationPlanning.ts`、`frontend/src/views/smart_map/useSmartMapRenderer.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| renderer 仍通过页面 wrapper 读取溯源候选和精修图层状态 | `useSmartMapSourceWorkflowState.ts` 返回 `sourceWorkflowLayerState`，`useSmartMapRefinementPlayback.ts` 返回 `refinementLayerState`，renderer 直接消费候选区域、选中候选、预测源点、当前精修迭代和强调状态；`smart_map/index.vue` 降至 2483 行，质量审计阈值收紧到 2483 并禁止旧溯源 overlay wrapper 回填 | `frontend/src/views/smart_map/useSmartMapSourceWorkflowState.ts`、`frontend/src/views/smart_map/useSmartMapRefinementPlayback.ts`、`frontend/src/views/smart_map/useSmartMapRenderer.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| renderer 仍通过页面分散注入核心传感器和风险网格 getter | `useSmartMapCoreState.ts` 返回 `coreLayerState`，renderer 直接消费传感器列表、风险网格和选中传感器 id getter；`smart_map/index.vue` 降至 2481 行，质量审计阈值收紧到 2481 并禁止旧选中传感器 renderer 接线回填 | `frontend/src/views/smart_map/useSmartMapCoreState.ts`、`frontend/src/views/smart_map/useSmartMapRenderer.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| renderer 仍通过页面分散注入 hit-testing 派生 getter | `useSmartMapHitTesting.ts` 暴露 `SmartMapHitTestingLayer`，renderer 直接消费筛选匹配、设施边界和可见入口 getter；`smart_map/index.vue` 降至 2479 行，质量审计阈值收紧到 2479 并禁止旧 hit-testing renderer 接线回填 | `frontend/src/views/smart_map/useSmartMapHitTesting.ts`、`frontend/src/views/smart_map/useSmartMapRenderer.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| renderer 仍通过页面分散注入传感器渲染规则 | `smartMapSensorDimensions.ts` 暴露 `createSmartMapSensorRenderRules()`，renderer 直接消费传感器类型、默认监测范围、范围解析和优先级颜色；`smart_map/index.vue` 降至 2478 行，质量审计阈值收紧到 2478 并禁止旧传感器渲染规则接线回填 | `frontend/src/views/smart_map/smartMapSensorDimensions.ts`、`frontend/src/views/smart_map/useSmartMapRenderer.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| renderer 仍通过页面分散注入 viewport 裁剪动作 | `useSmartMapViewport.ts` 返回 `viewportRenderControls`，renderer 直接消费地图边界裁剪动作；`smart_map/index.vue` 保持 2478 行，质量审计禁止旧 viewport renderer 接线回填 | `frontend/src/views/smart_map/useSmartMapViewport.ts`、`frontend/src/views/smart_map/useSmartMapRenderer.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 传感器编辑仍保留无意义函数转发闭包 | 传感器编辑模块改为直接接收 `buildFrameSeriesTemplate` 和 `normalizeManualSeries` 函数引用；保留有初始化顺序依赖的闭包不动；`smart_map/index.vue` 保持 2478 行，质量审计禁止安全可移除的转发闭包回填 | `frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| workflow 和扩散模拟仍通过页面闭包读取运行状态 | `useSmartMapDiffusionPlayback.ts` 返回 `diffusionRunState`，`useSmartMapAlgorithmStates.ts` 返回 `sourceInversionRunState`，workflow 和扩散模拟模块直接消费运行状态对象；`smart_map/index.vue` 保持 2478 行，质量审计禁止旧运行状态 wrapper 回填 | `frontend/src/views/smart_map/useSmartMapDiffusionPlayback.ts`、`frontend/src/views/smart_map/useSmartMapAlgorithmStates.ts`、`frontend/src/views/smart_map/useSmartMapWorkflowSteps.ts`、`frontend/src/views/smart_map/useSmartMapDiffusionSimulation.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 测距工具仍通过页面转发 toast 包装闭包 | `useSmartMapMeasureTool` 直接接收现有 `showToast` 函数引用，页面删除无意义转发闭包；`smart_map/index.vue` 降至 2476 行，质量审计阈值收紧到 2476 | `frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| `docs/references` README 仍把空目录写成已有算法参考资料归档 | README 增加当前库存说明：`policies/`、`equipment/` 有已提交资料，`algorithms/`、`miscellaneous/` 只是占位，不能作为算法验证证据 | `docs/references/README.md` |
| 环境读数接口和页面空态仍使用“实测环境数据”措辞 | 改为“环境观测/外部环境观测”，避免把和风天气、人工录入或测试来源写成园区硬件实测链路；质量审计禁止旧措辞回填 | `EnvironmentReadingController`、`MonitoringDataController`、`monitor.vue`、`emergency/index.vue`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 继续内联入口点组合类型 | `useSmartMapEntranceCanvas.ts` 暴露 `SmartMapEntranceRecord`，页面删除本地 `MapPoint` / `EntranceLike` 组合类型；`smart_map/index.vue` 降至 2467 行，质量审计阈值收紧到 2467 | `frontend/src/views/smart_map/useSmartMapEntranceCanvas.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 根 README 生产环境变量示例漏写应用数据库密码 | README 的部署变量示例补 `MYSQL_APP_PASSWORD`，并用质量审计锁定 README、`deploy/README.md`、`deploy/.env.example` 与 compose 的部署变量口径一致，禁止回到虚构 `analysis-service:8100` | `README.md`、`deploy/README.md`、`deploy/.env.example`、`tools/code_quality_audit.py` |
| 根 README、技术路线和数据来源文档仍混用旧 Python 包入口 | 算法验证命令统一从仓库根目录使用 `python -m algorithm.*`；质量审计禁止当前文档回退到旧的裸子包入口 | `README.md`、`docs/technical-route-to-deployment.md`、`docs/dataset-sources.md`、`algorithm/README.md`、`tools/code_quality_audit.py` |
| `项目总体要求` 仍在部分章节把小车视频识别写成当前能力 | 小车链路统一为“JPG/PNG 图片识别已交付，视频识别需补帧抽取、标注样本和指标报告后才可扩展”，质量审计禁止 `图片/视频` 混写回流 | `docs/项目总体要求.md`、`tools/code_quality_audit.py` |
| 根 README 仍把固定传感器/小车采集浓度写成当前链路 | README 改为“目标采集能力”和“当前仿真采样/手工观测/巡检图片素材”分离；质量审计禁止旧句回填 | `README.md`、`tools/code_quality_audit.py` |
| `项目总体要求` 开头仍可能把固定传感器/小车采集写成当前已接入能力 | 总体要求改为“目标能力/当前仓库边界”分离：当前仓库尚未接入真实硬件采集链路，现有观测只能作为仿真采样、手工观测或巡检图片识别链路，不能写成现场实测；质量审计同步锁住 | `docs/项目总体要求.md`、`tools/code_quality_audit.py` |
| 仿真/环境监测 controller 仍直接持有 mapper，业务规则挂在 API 层 | 新增 `EnvironmentReadingService`、`SimulationMonitoringService`，把 limit clamp、仿真来源/质量状态校验和持久化下沉到 service；controller 只做接口委派，质量审计禁止 mapper 回流 | `backend/src/main/java/com/at/controller/EnvironmentReadingController.java`、`backend/src/main/java/com/at/controller/SimulationMonitoringController.java`、`backend/src/main/java/com/at/service/EnvironmentReadingService.java`、`backend/src/main/java/com/at/service/SimulationMonitoringService.java`、`tools/code_quality_audit.py` |
| `ImageAnalysisController` 仍直接持有巡检记录/小车 mapper，并在 API 层承担 YOLO 代理归一化、巡检记录保存和汇总统计 | 新增 `ImageAnalysisService` 与 `ImageAnalysisException`，把文件校验、YOLO 代理请求、算法 envelope 归一化、巡检记录原子保存、列表 DTO、汇总和删除判断下沉到 service；controller 只做 `Result` 映射；质量审计禁止 mapper、`RestTemplate`、`RequestContext`、归一化/保存方法回流 controller | `backend/src/main/java/com/at/controller/ImageAnalysisController.java`、`backend/src/main/java/com/at/service/ImageAnalysisService.java`、`backend/src/main/java/com/at/service/ImageAnalysisException.java`、`backend/src/test/java/com/at/controller/ImageAnalysisControllerTest.java`、`backend/src/test/java/com/at/service/ImageAnalysisServiceTest.java`、`tools/code_quality_audit.py` |
| `MonitoringDataController` 仍直接持有 mapper/service 混合依赖，并把监测概览聚合规则挂在 API 层 | 新增 `MonitoringDataService` 与 `MonitoringOverviewDTO`，把传感器读数、环境观测、风险/在线数/预警车统计和天气文案构建下沉到 service；读数/趋势只来自 `sensor_reading`，无采样时返回空态；controller 只做 `/overview` 委托和日志；质量审计禁止 mapper 与聚合 helper 回流 controller | `backend/src/main/java/com/at/controller/MonitoringDataController.java`、`backend/src/main/java/com/at/service/MonitoringDataService.java`、`backend/src/main/java/com/at/pojo/dto/MonitoringOverviewDTO.java`、`backend/src/test/java/com/at/controller/MonitoringDataControllerTest.java`、`backend/src/test/java/com/at/service/MonitoringDataServiceTest.java`、`tools/code_quality_audit.py` |
| `user` 聚合被登录/注册链和管理员用户管理链拆成两套 mapper | 删除 `LoginMapper`，`LoginServiceImpl` 改为复用 `UserMapper.selectByUsername/insert`；注册仍强制普通用户角色，管理员创建继续校验角色和重复用户名；质量审计禁止 `LoginMapper`、`getByUsername` 和旧“双 mapper 职责分离”口径回流 | `backend/src/main/java/com/at/service/impl/LoginServiceImpl.java`、`backend/src/main/java/com/at/mapper/UserMapper.java`、`backend/src/test/java/com/at/service/impl/LoginServiceImplTest.java`、`backend/src/test/java/com/at/service/UserServiceTest.java`、`tools/code_quality_audit.py` |
| Canvas interaction 仍通过页面 wrapper 固定泄漏源点选来源 | `useSmartMapLeakSource` 暴露 `applyMapLeakSourcePoint()`，Canvas interaction 接口只接收语义化点选动作；页面删除 `point => applyLeakSourcePoint(point, 'map')` wrapper | `frontend/src/views/smart_map/useSmartMapLeakSource.ts`、`frontend/src/views/smart_map/useSmartMapCanvasInteraction.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 设施信息展示仍由页面 wrapper 注入 `zones` | `useSmartMapInfoPanel` 初始化时接收 `zones`，向外暴露单参 `showFacilityInfo(facility)`；`useSmartMapPageActions` 不再保存 zones 副本，页面删除 `facility => showFacilityInfoPanel(facility, zones)` wrapper | `frontend/src/views/smart_map/useSmartMapInfoPanel.ts`、`frontend/src/views/smart_map/useSmartMapPageActions.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 主算法服务 `inputSummary` 对当前嵌套观测 payload 摘要不足 | `_input_summary()` 识别 `observationPayload.sensors/activeSensors/frames`、顶层 `activeSensors`、候选区和 `refinementConfig`，测试覆盖嵌套观测载荷，避免只返回空传感器数量 | `algorithm/api_server.py`、`algorithm/tests/test_api_server_http_semantics.py`、`tools/code_quality_audit.py` |
| `smart_map/index.vue` 重复内联最近设施查找 wrapper | 页面保留一个 `findNearestFacility` 函数引用，传感器序列、手动布点和批量导入共用同一入口；质量审计禁止三处 inline wrapper 回填 | `frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 疏散规划仍由页面 wrapper 读取当前设施 id | `useSmartMapEvacuationPlanning` 直接接收 `selectedFacility` ref 并在内部解析当前设施 id；页面删除 `getSelectedFacilityId` wrapper，质量审计禁止回填 | `frontend/src/views/smart_map/useSmartMapEvacuationPlanning.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 风险网格和小车交互仍保留无意义页面 wrapper | `useSmartMapRiskGridActions` 直接接收 `weatherState` ref，`useSmartMapCarInteraction` 直接接收 `router` 并复用页面动作模块的详情导航实现；页面删除 `getWeather: () => weatherState.value` 与 `carId => navigateToSmartMapCarDetail(router, carId)` 转发，质量审计禁止回填 | `frontend/src/views/smart_map/useSmartMapRiskGrid.ts`、`frontend/src/views/smart_map/useSmartMapCarInteraction.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 覆盖率计算和观测摘要仍保留可安全消除的页面 wrapper | `useSmartMapRiskSummary` 收窄 `resolveRange` 签名后直接接收 `calculateSmartMapSensorCoverage`；`useSmartMapSelectionDisplayState` 移到观测构建器之后并直接接收观测 getter，页面删除三段函数转发，质量审计禁止回填 | `frontend/src/views/smart_map/useSmartMapRiskSummary.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 最近设施查找又回流成本地 lambda | `useSmartMapSensorPlacementRules.ts` 暴露 `createSmartMapNearestFacilityLookup()` 绑定设施列表，页面删除 `findNearestFacility = (x, y) => findNearestSmartMapFacility(x, y, facilities)`，质量审计禁止回填 | `frontend/src/views/smart_map/useSmartMapSensorPlacementRules.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 泄漏源模块仍通过页面取消布点 wrapper 解决初始化顺序 | `useSmartMapSensorPlacement.ts` 新增 `useSmartMapSensorPlacementCancelBridge()`，泄漏源模块接收稳定 bridge 函数，真实布点模块初始化后绑定取消动作；页面删除 `() => cancelSensorPicking()` / `() => cancelSensorOriginPicking()` wrapper，质量审计禁止回填 | `frontend/src/views/smart_map/useSmartMapSensorPlacement.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 溯源 workflow 仍通过页面 wrapper 调用粒子滤波历史构造 | `useSmartMapSourceWorkflowState.ts` 直接复用 `useSmartMapInversion` 的粒子滤波历史构造纯函数，并接收 `candidateRadius`；`useSmartMapObservationBuilders.ts` 删除无关转发职责，页面删除 `result => buildParticleFilterHistoryIterations(result)` wrapper，质量审计禁止回填 | `frontend/src/views/smart_map/useSmartMapSourceWorkflowState.ts`、`frontend/src/views/smart_map/useSmartMapObservationBuilders.ts`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 设备图片全屏查看器模板和样式仍留在 `smart_map/index.vue` / `index.css` | 新增 `SmartMapDeviceFullscreen.vue`，把全屏浮层 DOM、缩放工具栏、设备信息区和对应 scoped 样式移出页面；父页面只保留状态与事件绑定，质量审计要求组件存在并禁止全屏 DOM/CSS 回流 | `frontend/src/views/smart_map/components/SmartMapDeviceFullscreen.vue`、`frontend/src/views/smart_map/index.vue`、`frontend/src/views/smart_map/index.css`、`tools/code_quality_audit.py` |
| 传感器编辑弹窗模板和样式仍留在 `smart_map/index.vue` / `index.css` | 新增 `SmartMapSensorEditDialog.vue`，把编辑弹窗 DOM、字段绑定和对应 scoped 样式移出页面；父页面只保留 draft/state/save 事件接线，`smart_map/index.vue` 降至 2389 行，质量审计要求组件存在并禁止弹窗 DOM/CSS 回流 | `frontend/src/views/smart_map/components/SmartMapSensorEditDialog.vue`、`frontend/src/views/smart_map/useSmartMapSensorEditor.ts`、`frontend/src/views/smart_map/index.vue`、`frontend/src/views/smart_map/index.css`、`tools/code_quality_audit.py` |
| 传感器悬浮卡片 view model 已抽出但模板和样式仍留在 `smart_map/index.vue` / `index.css` | 新增 `SmartMapSensorHoverCard.vue`，把悬浮卡片 DOM、风险颜色展示和对应 scoped 样式移出页面；父页面只传 `hoveredSensorCard` 与既有优先级颜色函数，`smart_map/index.vue` 降至 2375 行，质量审计要求组件存在并禁止 hover DOM/CSS 回流 | `frontend/src/views/smart_map/components/SmartMapSensorHoverCard.vue`、`frontend/src/views/smart_map/useSmartMapSensorHoverCard.ts`、`frontend/src/views/smart_map/index.vue`、`frontend/src/views/smart_map/index.css`、`tools/code_quality_audit.py` |
| Toast 状态已抽出但展示 DOM 和样式仍留在 `smart_map/index.vue` / `index.css` | 新增 `SmartMapToast.vue`，把提示条 DOM、动画和图标颜色样式移出页面；父页面只传现有 toast 状态，`smart_map/index.css` 降至 888 行，质量审计要求组件存在并禁止 toast DOM/CSS 回流 | `frontend/src/views/smart_map/components/SmartMapToast.vue`、`frontend/src/views/smart_map/useSmartMapToast.ts`、`frontend/src/views/smart_map/index.vue`、`frontend/src/views/smart_map/index.css`、`tools/code_quality_audit.py` |
| 扩散时间轴模板和大部分样式仍留在 `smart_map/index.vue` / `index.css` | 新增 `SmartMapDiffusionTimeline.vue`，把扩散播放面板 DOM、播放按钮、速度/循环控件和对应 scoped 样式移出页面；父页面只传播放状态、summary 和事件处理，保留全局 `.timeline-slider` 给精修滑条复用，`smart_map/index.vue` 降至 2353 行、`index.css` 降至 808 行，质量审计禁止旧时间轴 DOM/CSS 回流 | `frontend/src/views/smart_map/components/SmartMapDiffusionTimeline.vue`、`frontend/src/views/smart_map/index.vue`、`frontend/src/views/smart_map/index.css`、`tools/code_quality_audit.py` |
| 地图缩放按钮和比例尺模板/样式仍留在 `smart_map/index.vue` / `index.css` | 新增 `SmartMapViewportControls.vue`，把比例尺、缩放、重置和标签开关按钮移出页面；父页面只传 `showLabels` 和既有缩放/标签事件，`smart_map/index.vue` 降至 2350 行、`index.css` 降至 781 行，质量审计禁止旧 viewport 控件 DOM/CSS 回流 | `frontend/src/views/smart_map/components/SmartMapViewportControls.vue`、`frontend/src/views/smart_map/useSmartMapViewControls.ts`、`frontend/src/views/smart_map/index.vue`、`frontend/src/views/smart_map/index.css`、`tools/code_quality_audit.py` |
| 坐标显示模板和样式仍留在 `smart_map/index.vue` / `index.css` | 新增 `SmartMapCoordinateDisplay.vue`，把经度、纬度、海拔展示 DOM 与对应 scoped 样式移出页面；父页面只传 `coordLongitude/coordLatitude/coordAltitude`，`smart_map/index.vue` 降至 2342 行、`index.css` 降至 759 行，质量审计禁止坐标 DOM/CSS 回流 | `frontend/src/views/smart_map/components/SmartMapCoordinateDisplay.vue`、`frontend/src/views/smart_map/index.vue`、`frontend/src/views/smart_map/index.css`、`tools/code_quality_audit.py` |
| 底部地图工具条模板和定位样式仍留在 `smart_map/index.vue` / `index.css` | 新增 `SmartMapBottomToolbar.vue`，把选择、出入口、测距、传感器和半径范围按钮 DOM 与工具条定位样式移出页面；`tool-btn` 基础样式因气体编辑按钮仍复用而暂留主样式，`smart_map/index.vue` 降至 2336 行、`index.css` 降至 755 行，质量审计禁止底部工具条 DOM/CSS 回流 | `frontend/src/views/smart_map/components/SmartMapBottomToolbar.vue`、`frontend/src/views/smart_map/index.vue`、`frontend/src/views/smart_map/index.css`、`tools/code_quality_audit.py` |
| 左侧搜索框模板和样式仍留在 `smart_map/index.vue` / `index.css` | 新增 `SmartMapSearchBox.vue`，把搜索框 DOM、输入同步和对应 scoped 样式移出页面；父页面只保留 `searchQuery` 和 `onSearch` 接线，`smart_map/index.vue` 降至 2334 行、`index.css` 降至 741 行，质量审计禁止搜索框 DOM/CSS 回流 | `frontend/src/views/smart_map/components/SmartMapSearchBox.vue`、`frontend/src/views/smart_map/index.vue`、`frontend/src/views/smart_map/index.css`、`tools/code_quality_audit.py` |
| 左侧图例列表模板和样式仍留在 `smart_map/index.vue` / `index.css` | 新增 `SmartMapLegendList.vue`，把图例列表 DOM、色块样式和 hover 样式移出页面；父页面只传 `legends`，`smart_map/index.vue` 降至 2330 行、`index.css` 降至 731 行，质量审计禁止图例 DOM/CSS 回流 | `frontend/src/views/smart_map/components/SmartMapLegendList.vue`、`frontend/src/views/smart_map/index.vue`、`frontend/src/views/smart_map/index.css`、`tools/code_quality_audit.py` |
| 左侧功能分区列表模板和样式仍留在 `smart_map/index.vue` / `index.css` | 新增 `SmartMapZoneList.vue`，把分区列表 DOM、色块、选中态和标签样式移出页面；父页面只传 `zones/selectedZone` 和选择事件，`smart_map/index.vue` 降至 2322 行、`index.css` 降至 717 行，质量审计禁止分区列表 DOM/CSS 回流 | `frontend/src/views/smart_map/components/SmartMapZoneList.vue`、`frontend/src/views/smart_map/index.vue`、`frontend/src/views/smart_map/index.css`、`tools/code_quality_audit.py` |
| 左侧园区概览统计卡片模板和样式仍留在 `smart_map/index.vue` / `index.css` | 新增 `SmartMapStatsGrid.vue`，把统计卡片 DOM、激活态和数值样式移出页面；父页面只传 `stats/activeFilter` 和筛选事件，`smart_map/index.vue` 降至 2317 行、`index.css` 降至 703 行，质量审计禁止统计卡片 DOM/CSS 回流 | `frontend/src/views/smart_map/components/SmartMapStatsGrid.vue`、`frontend/src/views/smart_map/index.vue`、`frontend/src/views/smart_map/index.css`、`tools/code_quality_audit.py` |
| 右侧实时告警列表模板和样式仍留在 `smart_map/index.vue` / `index.css` | 新增 `SmartMapAlertList.vue`，把告警列表 DOM、告警类型颜色和时间文本样式移出页面；父页面只传 `alerts`，`smart_map/index.vue` 降至 2310 行、`index.css` 降至 682 行，质量审计禁止告警列表 DOM/CSS 回流 | `frontend/src/views/smart_map/components/SmartMapAlertList.vue`、`frontend/src/views/smart_map/index.vue`、`frontend/src/views/smart_map/index.css`、`tools/code_quality_audit.py` |
| 区域风险等级统计模板和样式仍留在 `smart_map/index.vue` / `index.css` | 新增 `SmartMapRiskStats.vue`，把四级风险统计 DOM、颜色点和数量样式移出页面；父页面只传 `riskStat`，`smart_map/index.vue` 降至 2290 行、`index.css` 降至 673 行，质量审计禁止风险统计 DOM/CSS 回流 | `frontend/src/views/smart_map/components/SmartMapRiskStats.vue`、`frontend/src/views/smart_map/index.vue`、`frontend/src/views/smart_map/index.css`、`tools/code_quality_audit.py` |
| 气象环境面板模板和内联样式仍留在 `smart_map/index.vue` | 新增 `SmartMapWeatherPanel.vue`，把天气来源标签、风速/风向/温度卡片和观测时间展示移出页面；保留 `实时天气/模拟天气` 语义，`smart_map/index.vue` 降至 2276 行，质量审计禁止旧天气模板和内联样式回流 | `frontend/src/views/smart_map/components/SmartMapWeatherPanel.vue`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| 传感器布局统计模板和样式仍留在 `smart_map/index.vue` / `index.css` | 新增 `SmartMapLayoutStats.vue`，把传感器总数、全域覆盖率、高风险覆盖率和部署成本四项统计移出页面；父页面只传 `layoutResult`，`smart_map/index.vue` 降至 2260 行、`index.css` 降至 661 行，质量审计禁止布局统计 DOM/CSS 回流 | `frontend/src/views/smart_map/components/SmartMapLayoutStats.vue`、`frontend/src/views/smart_map/index.vue`、`frontend/src/views/smart_map/index.css`、`tools/code_quality_audit.py` |
| 传感器布局操作按钮组仍以内联样式留在 `smart_map/index.vue` | 新增 `SmartMapLayoutActions.vue`，把手动添加、手动录入、清空全部三按钮和网格布局样式移出页面；父页面只传按钮状态和事件，`smart_map/index.vue` 降至 2257 行，质量审计禁止按钮组内联模板回流 | `frontend/src/views/smart_map/components/SmartMapLayoutActions.vue`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |
| YOLO 检测结果卡片模板和样式仍留在 `smart_map/index.vue` / `index.css` | 新增 `SmartMapYoloResultCard.vue`，把检测结果标题、人数、图片、检测时间和模型版本展示移出页面；父页面只传 `yoloResult` 与当前小车 id，`smart_map/index.vue` 降至 2249 行、`index.css` 降至 640 行，质量审计禁止卡片 DOM/CSS 回流 | `frontend/src/views/smart_map/components/SmartMapYoloResultCard.vue`、`frontend/src/views/smart_map/index.vue`、`frontend/src/views/smart_map/index.css`、`tools/code_quality_audit.py` |
| 传感器历史浓度曲线 SVG 模板和样式仍留在 `smart_map/index.vue` / `index.css` | 新增 `SmartMapSensorHistoryChart.vue`，把历史浓度曲线标题、坐标轴、阈值线、当前帧标记和峰值摘要移出页面；父页面只传 `selectedSensorHistoryChart`，`smart_map/index.vue` 降至 2196 行、`index.css` 降至 603 行，质量审计禁止曲线 SVG 模板/CSS 回流 | `frontend/src/views/smart_map/components/SmartMapSensorHistoryChart.vue`、`frontend/src/views/smart_map/index.vue`、`frontend/src/views/smart_map/index.css`、`tools/code_quality_audit.py` |
| 传感器设备安装详情卡片模板和样式仍留在 `smart_map/index.vue` / `index.css` | 新增 `SmartMapSensorDeviceCard.vue`，把设备图片、在线状态、浓度摘要和全屏按钮移出页面；父页面只传 `sensorDeviceCard` 并监听全屏事件，`smart_map/index.vue` 降至 2181 行、`index.css` 降至 533 行，质量审计禁止设备卡片 DOM/CSS 回流 | `frontend/src/views/smart_map/components/SmartMapSensorDeviceCard.vue`、`frontend/src/views/smart_map/index.vue`、`frontend/src/views/smart_map/index.css`、`tools/code_quality_audit.py` |
| 传感器采样摘要仍以内联模板留在 `smart_map/index.vue` | 新增 `SmartMapSamplingSummary.vue`，把当前帧读数、预警和危险计数展示移出页面；父页面只传 `sensorSamplingSummary`，`smart_map/index.vue` 降至 1474 行，质量审计禁止采样摘要文案回流 | `frontend/src/views/smart_map/components/SmartMapSamplingSummary.vue`、`frontend/src/views/smart_map/index.vue`、`tools/code_quality_audit.py` |

## 仍未完成

| 优先级 | 剩余问题 | 下一步 |
| --- | --- | --- |
| P0 | `smart_map` 仍是巨型前端微内核 | 已抽出 YOLO、溯源载荷、溯源 workflow 状态、观测 payload 动作、监测/扩散摘要状态、选择展示状态、轻量浓度估算工具、传感器尺寸 resolver、传感器聚焦视图计算、小车交互状态、算法 API 执行包装、算法执行器/配置状态、扩散场景状态、气象展示状态、疏散规划状态、流程进度状态、扩散播放状态、溯源精修播放状态、扩散算法调用编排、气体编辑状态、toast 状态、视图控制状态、时钟/坐标展示状态、右侧信息面板状态、小车详情面板、传感器悬浮卡片、覆盖率/风险统计状态、UI/错误辅助函数、传感器/气体持久化、验证报告、观测来源语义、泄漏源选择、布点风险规则、风险网格/覆盖统计、标准布点/编号生成、手动新增传感器保存编排、画布视图状态、命中测试、设施信息面板、传感器详情面板、测距工具、基础 Canvas 底图/标签/热力图、专项 Canvas 绘制子模块、图层级渲染适配、Canvas runtime、Canvas shell、核心根状态、lifecycle coordinator、主 renderer、render bridge、地图图片加载桥接、runtime 绑定桥接、后置 page actions、风险网格 wrapper、传感器详情动作体、传感器详情 action bridge、传感器编辑同步 bridge、气体删除确认 bridge、泄漏源点选取消动作、3D 设施选择 action、本地导航/信息清理 glue、Canvas 选择分发动作、hit-test 状态读取 wrapper、最近设施查找薄包装、死 route hook 清理、扩散帧 `as unknown` 类型转换清理、泄漏源 getter 重复注入清理、测距 toast 转发闭包清理、入口点组合类型清理、天气状态读取 wrapper 清理、小车详情导航 wrapper 清理、覆盖率计算 wrapper 清理、观测摘要 getter wrapper 清理、最近设施本地 lambda 清理、传感器点选取消 wrapper 清理、粒子滤波历史构造 wrapper 清理、设备全屏组件模板/样式拆分、传感器编辑弹窗组件模板/样式拆分、传感器悬浮卡片组件模板/样式拆分、Toast 展示组件模板/样式拆分、扩散时间轴组件模板/样式拆分、viewport 控件组件模板/样式拆分、坐标显示组件模板/样式拆分、底部工具条组件模板/定位样式拆分、搜索框组件模板/样式拆分、图例列表组件模板/样式拆分、功能分区列表组件模板/样式拆分、园区概览统计卡片组件模板/样式拆分、实时告警列表组件模板/样式拆分、区域风险统计组件模板/样式拆分、气象环境面板组件模板/内联样式拆分、传感器布局统计组件模板/样式拆分、传感器布局操作按钮组件拆分、YOLO 结果卡片组件模板/样式拆分、传感器历史浓度曲线 SVG 组件拆分和传感器设备安装详情卡片组件拆分；扩散元数据和疏散结果状态已从 `AnyRecord` 收窄为领域接口；下一步继续拆剩余模板级组件和早期注入 glue |
| P1 | 核心算法真实验证次数仍不足 | 继续接入更多公开实验/现场样本；当前矩阵已承认缺口，不能把合成回归写成 5 次真实验证 |

## 真实性边界

当前项目没有硬件采集链路，因此本批只把“实时监测”推进为“可追溯仿真监测链路”。凡是由仓库生成的读数，必须显式保存为 `source=simulation` 与 `quality_status=SIMULATED`；不能写成真实实测。
