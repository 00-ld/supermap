# 化工园区危险气体扩散与溯源系统项目总体要求

> 本文档作为项目需求、架构设计、开发实现、测试验证、部署运维的总体约束文件。后续代码、数据库、接口、算法、页面和文档均应遵循本文档；若实际实现与本文档冲突，应先更新本文档并记录变更原因。

## 1. 项目定位

本项目围绕化工园区危险气体安全管控，建设集“危险气体扩散模拟、泄漏源溯源、传感器监测、巡检小车动态采样、逃生路径规划、二维/三维数字孪生可视化、安全联动决策”于一体的综合平台。

系统应支持在二维或三维数字孪生底座上选取泄漏点位，配置风速、风向、气体类型、泄漏速率、环境参数、气体物理参数等条件，模拟典型泄漏场景下的危险气体扩散过程；目标能力是结合固定传感器与阿克曼巡检小车采集的浓度数据进行泄漏源反演溯源，并根据气体扩散浓度场动态生成最优逃生路径。当前仓库尚未接入真实硬件采集链路，现有观测只能作为仿真采样、手工观测或巡检图片识别链路使用，不能写成现场实测。

## 2. 核心目标

1. 建立经过真实性验证的危险气体扩散模型，支持化工园区典型泄漏场景模拟。
2. 建立基于多点位浓度数据的泄漏源溯源模型，输出泄漏点位的估计位置、置信度和误差范围。
3. 建立依据国家标准或行业规范的传感器监控点位布局方案，支持固定点位与动态巡检点位协同。
4. 建立基于仿真浓度场和人工确认事件的逃生路径规划能力；未接入真实采集链路前，路径更新只能标注为仿真/演练结果。
5. 建立二维与三维联动的数字孪生展示能力，二维优先承载监控点位布局、浓度热力图和路径规划，三维作为最终核心展示目标。
6. 建立规范化 API、数据库、算法、日志、安全和部署约束，使项目具备持续维护和服务器部署能力。

## 3. 技术栈约束

### 3.1 前端

- 使用 Vue 3 作为前端主体框架。
- 使用 ECharts 承载统计图表、趋势曲线、浓度分布图等可视化内容。
- 使用 Canvas 承载二维园区平面、浓度热力图、监控点位、路径规划等高频绘制内容。
- 前端应模块化组织页面、组件、API 请求、状态管理和工具方法。
- 前端所有接口调用必须通过统一请求封装，不允许在页面组件中散落硬编码接口地址。

### 3.2 后端与算法服务

- 当前主业务后端保留 Java Spring Boot，不在本阶段迁移为 Python。
- Python 主要用于算法服务，推荐使用 FastAPI 承载扩散、溯源、路径规划和 YOLO 推理接口。
- Java 后端与 Python 算法服务必须遵循统一接口协议；对外业务接口、算法接口、设备接口和管理接口均应返回统一响应结构或由网关/后端适配为统一结构。
- 后端应分层清晰，至少包含 API 层、服务层、业务逻辑层、工具层、算法调用层和数据访问层。
- 若后续重新启动 Java 到 Python 的迁移，必须另起迁移计划，明确模块映射、接口兼容、数据迁移、测试和回滚方案。

### 3.3 三维数字孪生

- 三维底座可使用 SuperMap 或 Three.js。
- 已在 SuperMap iPortal 中建设的数字大屏应作为三维数字孪生的重要展示入口，后端需要为 iPortal 数字大屏提供稳定、统一、标准化的 JSON 数据接口。
- 三维展示应支持园区建筑、道路、设备、监控点位、巡检小车、泄漏点、浓度云图或体渲染效果。
- 三维坐标与二维坐标必须建立统一映射关系，所有点位数据必须具备可追溯坐标来源。

### 3.4 数据库

- 使用 MySQL 作为主要业务数据库。
- 项目根目录必须包含 `db/` 文件夹，用于存放数据库初始化脚本、迁移脚本、种子数据、测试数据说明和 ER 图。
- 数据库字符集必须统一使用 `utf8mb4`，排序规则建议使用 `utf8mb4_unicode_ci` 或项目统一约定的兼容规则。

### 3.5 AI 与开发辅助技术

- 可使用 OpenAI、Claude Code、DeepSeek、Mimo 等 AI 技术辅助开发、文档生成、代码审查、算法实验和测试数据分析。
- 后续可接入千问大模型，用于逃生规范建议、事故处理建议、应急预案问答、异常数据解释和事故报告辅助生成。
- AI 生成的代码、算法结论、测试报告和文档必须经过人工复核。
- 涉及安全决策、人员疏散、泄漏研判的结果必须保持客观表达，不得夸大模型能力。
- 大模型输出只能作为辅助建议，不得直接替代扩散模型、溯源模型、路径规划算法和现场应急负责人决策。

### 3.6 硬件

- 固定气体传感器作为目标静态监控点位；当前仓库的固定点位数据必须标明来源和质量状态。
- 阿克曼巡检小车作为目标动态监控点位；当前仓库没有真实车载气体传感器连续采样链路。
- 小车目标能力是搭载气体传感器和摄像头；当前已落地的是巡检图片识别链路，不得把它写成视频或真实浓度采集已交付。
- 小车摄像头上传的 JPG/PNG 图片可通过 YOLO11m 算法识别人员位置；视频识别需先补齐帧抽取、标注样本和指标报告，当前不得写成已交付能力。

## 4. 监测气体范围

系统首期必须支持以下四类气体浓度数据：

| 气体 | 标识 | 说明 |
| --- | --- | --- |
| 一氧化碳 | CO | 有毒气体 |
| 氧气 | O2 | 环境氧含量，需关注缺氧或富氧风险 |
| 氨气 | NH3 | 有毒、有刺激性气体 |
| 甲烷 | CH4 | 易燃易爆气体 |

所有气体数据必须包含单位、采样时间、采样点位、数据来源、设备编号和质量标记。

## 5. 总体架构分层

项目至少包含以下逻辑层次，各层职责必须清晰，禁止跨层直接耦合。

```text
前端展示层
  -> API 接口层
  -> 业务逻辑层
  -> 服务层
  -> 算法实现层
  -> tools 工具层
  -> 数据访问层
  -> MySQL / 文件 / 模型 / 日志
  -> 三维可视化层
```

### 5.1 项目根目录规范化子文件夹

项目根目录下必须建立清晰、稳定、可维护的子文件夹结构。所有新增代码、数据、文档、模型、脚本和部署配置都应放入对应目录，禁止长期堆放在项目根目录。

建议根目录结构如下：

```text
.
  frontend/        # 前端工程，Vue 3、ECharts、Canvas、页面和组件
  backend/         # Java Spring Boot 主业务后端，统一接口、业务服务、权限认证
  algorithm/       # 算法实现，扩散、溯源、路径规划、YOLO 推理封装
  twin/            # 三维数字孪生，SuperMap、Three.js、iPortal 大屏接入
  db/              # MySQL 初始化脚本、迁移脚本、种子数据、ER 图
  datasets/        # 权威数据集、原始数据、清洗后数据、数据来源说明
  models/          # 训练模型、推理模型、模型版本说明，不提交超大敏感模型
  docs/            # 项目文档、接口文档、算法验证报告、部署文档
  tests/           # 单元测试、接口测试、算法测试、端到端测试
  tools/           # 通用工具脚本，不绑定具体业务流程
  scripts/         # 开发、构建、数据处理、部署辅助脚本
  docker/          # Dockerfile、容器配置、服务镜像构建文件
  deploy/          # 服务器部署、Nginx、HTTPS、域名、运维配置
  config/          # 配置模板，不存放真实密钥
  uploads/         # 本地上传文件临时目录，生产环境应挂载到独立存储
  logs/            # 本地运行日志目录，禁止提交真实生产日志
  assets/          # 图片、图标、地图静态资源、三维模型轻量资源
  .github/         # CI、代码检查、自动化流程
```

强制约束：

- 根目录只允许保留 README、项目配置文件和少量入口文件。
- 业务代码不得直接散落在根目录。
- 真实密钥、生产数据库备份、生产日志、未脱敏数据集不得提交到仓库。
- 若当前仓库存在历史目录名，例如 `Manage/`、`Back/`、`python/`、`GasModelTest/`、`img/`、`chemical-park-monitor/`，不得提交到 GitHub；如需保留参考材料，应整理进规范目录并说明来源。
- 每个一级目录应至少包含 `README.md` 或等价说明，解释该目录用途、维护规则和禁止事项。

文件夹命名规则：

- 一级目录必须使用小写英文名称，优先采用单数领域名，例如 `frontend/`、`backend/`、`algorithm/`、`tests/`、`docs/`、`deploy/`。
- 禁止新增中文目录名、空格目录名、大小写混用目录名和个人临时目录名。
- 历史目录名不得继续沿用：`Manage/` 应整理到 `frontend/`，`Back/` 应整理到 `backend/`，`algorithm_tests/` 应整理到 `tests/`，`python/` 应整理到 `algorithm/`，`chemical-park-monitor/` 应拆分归档到对应规范目录。
- 二级及以下目录应遵循所在技术栈约定：前端组件目录使用清晰英文语义，Python 模块使用 `snake_case`，Java 包目录使用小写英文。
- 若确需新增一级目录，必须先更新本节目录表、README 和审计规则，再提交代码。

## 6. 业务逻辑层约束

业务逻辑层负责组织系统核心业务流程，不直接处理底层数据库细节，不直接实现复杂算法。

必须覆盖以下业务：

1. 泄漏场景创建：选择泄漏点、气体类型、泄漏参数、环境参数。
2. 扩散模拟任务管理：创建任务、启动任务、暂停任务、查询进度、查看结果。
3. 浓度数据管理：接收固定传感器、小车动态采样和仿真模型输出数据。
4. 异常识别：根据阈值、趋势、空间分布判断异常浓度数据。
5. 泄漏源溯源：基于异常点位浓度数据调用溯源模型。
6. 安全决策联动：生成异常日志、风险等级、建议处置动作和联动通知。
7. 逃生路径规划：基于人员位置、道路拓扑、气体浓度场动态计算最优路径。
8. 小车任务管理：管理小车状态、位置、采样任务、摄像头识别结果和异常上报。

## 7. 服务层约束

服务层负责封装可复用业务能力，对 API 层提供稳定服务。

建议拆分为以下服务：

| 服务 | 职责 |
| --- | --- |
| SensorService | 传感器注册、状态、点位、采样数据处理 |
| GasDataService | 四类气体浓度数据入库、查询、统计、异常筛选 |
| DiffusionService | 扩散模型任务调度、参数校验、结果管理 |
| SourceInversionService | 溯源模型调用、结果解释、误差评估 |
| EvacuationService | D* Lite 路径规划、风险区域避让、路径更新 |
| VehicleService | 阿克曼小车管理、动态采样、视频/图片上传记录 |
| VisionService | YOLO11m 图片识别、人员位置输出；视频识别需先补帧抽取与验证集 |
| TwinService | 二维/三维坐标转换、园区对象管理、可视化数据聚合 |
| AlarmService | 异常日志、告警等级、联动处置记录 |
| UserService | 人员、角色、权限、登录认证 |

服务层不得直接返回数据库实体给前端，必须通过 DTO 或统一响应模型输出。

## 8. tools 层约束

tools 层用于存放无业务状态的通用工具，必须保持可复用、可测试、低耦合。

建议包含：

- 坐标转换工具：二维坐标、三维坐标、GIS 坐标转换。
- 单位换算工具：ppm、mg/m3、体积分数等单位转换。
- 时间处理工具：时区、时间戳、采样间隔、仿真步长处理。
- 数据校验工具：气体参数、环境参数、传感器数据合法性校验。
- 文件处理工具：上传文件、模型文件、数据集文件、日志文件处理。
- 加密与脱敏工具：密码哈希、敏感字段脱敏、token 处理。
- API 响应工具：统一响应、错误码、分页结构。
- 日志工具：业务日志、异常日志、算法运行日志、审计日志。

tools 层不得依赖页面、具体业务流程或数据库表结构。

## 9. 算法实现层约束

算法实现层是项目核心，应独立组织、可测试、可复现实验结果。

### 9.1 扩散模型

扩散模型必须支持：

- 泄漏点坐标。
- 气体类型。
- 泄漏速率或源强。
- 风速、风向。
- 温度、湿度、稳定度等环境参数。
- 地形或障碍物影响的扩展接口。
- 输出二维浓度场，预留三维浓度场能力。

扩散结果必须包含：

- 时间步。
- 空间坐标。
- 浓度值。
- 气体类型。
- 模型参数快照。
- 结果可信说明。

### 9.2 溯源模型

溯源模型必须支持基于多个监控点位浓度数据进行泄漏点反演。

输出至少包含：

- 估计泄漏点坐标。
- 估计源强。
- 置信度或可信区间。
- 与真实泄漏点的误差。
- 参与计算的传感器点位列表。
- 模型版本和参数快照。

### 9.3 逃生路径规划

逃生路径规划使用 D* Lite 算法作为核心动态路径规划算法。

路径规划必须考虑：

- 人员当前位置。
- 安全出口位置。
- 园区道路或可通行区域拓扑。
- 当前及预测浓度场。
- 高风险区域避让。
- 气体扩散导致的动态重规划。

当道路或区域浓度达到禁止通行阈值时，路径规划必须剔除该区域，不得仅通过降低权重继续通行。

### 9.4 YOLO11m 识别

小车管理页面当前必须支持上传小车摄像头拍摄的 JPG/PNG 图片，并调用 YOLO11m 算法识别人员位置；视频上传识别在补齐视频帧抽取、标注样本和指标报告前只能列为后续扩展，不得写成已交付能力。

识别结果应包含：

- 人员框坐标。
- 置信度。
- 图片或视频帧编号。
- 小车编号。
- 拍摄时间。
- 识别模型版本。

### 9.5 算法真实性验证

每个核心算法至少需要完成 5 次测试验证真实性。

验证要求：

1. 测试数据必须来自真实数据集、权威网站、权威平台、公开实验数据或可说明来源的现场采样数据。
2. 数据集必须下载并归档，不能只引用网页描述。
3. 每次验证必须记录数据来源、下载时间、数据版本、预处理方法、测试参数、指标结果和结论。
4. 验证结论必须保持客观，不得只写“效果良好”，必须说明误差、适用边界和失败情况。
5. 若使用仿真数据辅助训练，必须明确标注为仿真数据，不得当作真实数据。
6. 若对扩散模型进行 `diffusionCorrectionK` 标定，必须说明观测数据来源、候选 K 范围、评价指标、最优 K、适用边界和失败样例；合成观测只能作为回归测试，不能写成真实标定结果。

验证资产目录按以下口径维护：

```text
datasets/
  README.md                 # 已提交的数据目录说明
  samples/                  # 可提交的小体积、授权明确样本
  raw/                      # 本地下载原始数据，默认不提交
  processed/                # 本地预处理数据，默认不提交
  metadata/                 # 数据版本、许可证、下载记录，按数据集逐步补齐
tests/
  test_forward_model.py     # 当前已提交的合成回归入口
  case*/                    # 当前已提交的小体积合成样例
  diffusion/                # 目标测试分层，未建立前不得写成现状
  source_inversion/         # 目标测试分层，未建立前不得写成现状
  evacuation/               # 目标测试分层，未建立前不得写成现状
  vision/                   # 目标测试分层，未建立前不得写成现状
docs/
  algorithm-verification-report.md  # 已提交，记录真实验证、回归测试和缺口
```

## 10. 三维可视化层约束

三维可视化层负责展示园区数字孪生底座和安全态势。

必须支持：

- 园区建筑、道路、装置、罐区、管线或重点风险区域展示。
- 固定传感器点位展示。
- 阿克曼巡检小车位置展示。
- 泄漏点展示。
- 气体浓度场展示。
- 告警区域展示。
- 逃生路径展示。
- 二维与三维视角联动。
- SuperMap iPortal 数字大屏数据接入。

三维展示应优先保证信息准确和交互清晰，不得为了视觉效果牺牲安全信息可读性。

### 10.1 SuperMap iPortal 数字大屏接入

已建设的 SuperMap iPortal 数字大屏应纳入系统总体架构，作为园区态势展示和三维成果发布的重要入口。

后端必须为 iPortal 数字大屏提供标准化 JSON 数据接口，至少覆盖：

- 园区总体安全状态。
- 传感器点位数据及其来源标识。
- 四类气体浓度仿真/采样值和历史趋势。
- 异常告警列表。
- 泄漏点估计结果。
- 扩散浓度场概要数据。
- 小车巡检位置、任务状态及数据来源。
- 人员位置或区域统计。
- 逃生路径结果。
- 应急处置建议和联动状态。

iPortal 接口约束：

- 返回结构必须遵循本项目统一 JSON 响应协议。
- 坐标字段必须明确坐标系、二维坐标、三维坐标和来源。
- 时间字段必须使用 ISO 8601 格式并包含时区。
- 大屏高频刷新接口应控制返回体大小，必要时提供摘要接口和详情接口。
- 三维场景对象应包含稳定唯一 ID，便于前端或 iPortal 做增量更新。

## 11. API 接口层约束

### 11.1 统一接口前缀

当前已交付的 Java 后端接口统一使用 `/api/...` 路径，文档、前端调用和后端路由不得再写成未实现的版本化 v1 路径口径：

```text
/api/...
```

Python 算法服务当前按能力域暴露稳定接口：

```text
/api/diffusion/simulate
/api/inversion/...
/api/planning/evacuation
```

YOLO 图片识别服务当前接口为：

```text
/api/analysis/person
```

### 11.2 统一响应协议

所有 Java 后端和 Python 算法服务必须返回当前项目统一 JSON 外壳：

```json
{
  "code": 200,
  "message": "成功",
  "data": {},
  "ok": true,
  "timestamp": 1782262800000,
  "requestId": "uuid"
}
```

失败响应示例：

```json
{
  "code": 400,
  "message": "invalid gas type",
  "data": null,
  "ok": false,
  "timestamp": 1782262800000,
  "requestId": "uuid"
}
```

字段规范：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | number | 是 | 统一状态码，成功为 200，失败使用可文档化的 4xx/5xx 语义 |
| message | string | 是 | 给前端或调用方展示的简短说明 |
| data | object/array/null | 是 | 业务数据主体，失败时通常为 null |
| ok | boolean | 是 | 外层调用是否成功，通常与 `code === 200` 对齐 |
| timestamp | number | 是 | 服务端毫秒级时间戳 |
| requestId | string | 是 | 请求唯一标识，用于串联前端、后端、算法服务和日志 |

分页接口可在 `data` 内包含分页信息：

```json
{
  "page": 1,
  "pageSize": 20,
  "total": 100
}
```

算法接口的业务状态、错误细节和工程化 trace 元数据必须放在 `data` 内，不得在外层复制旧的 `success/error` 兼容字段。算法 `data` 至少包含：

```json
{
  "requestId": "uuid",
  "inputSummary": {
    "payloadDigest": "d23f4c0b9a71e2ad"
  },
  "algorithm": {
    "name": "source_inversion",
    "version": "v1.0.0",
    "configVersion": "config-2026-06"
  },
  "runtime": {
    "costMs": 43,
    "worker": "algorithm-worker-01"
  },
  "warnings": [],
  "errors": [],
  "grayRelease": {
    "enabled": false,
    "channel": "stable",
    "trafficPercent": 100,
    "rollbackTarget": "v1.0.0"
  },
  "fallback": {
    "used": false,
    "strategy": "NONE",
    "reason": null
  }
}
```

坐标类接口 `data` 或 `meta` 必须明确：

```json
{
  "coordinate_system": "park_local_3d",
  "x": 120.5,
  "y": 36.2,
  "z": 8.0,
  "source": "supermap_iportal"
}
```

后端、前端、iPortal 数字大屏、算法服务、小车设备服务均必须遵循同一套 JSON 返回规范。

### 11.3 错误码规范

错误码必须稳定、可检索、可文档化。

| 错误码 | 含义 |
| --- | --- |
| OK | 请求成功 |
| VALIDATION_ERROR | 参数校验失败 |
| AUTH_REQUIRED | 未登录或 token 缺失 |
| PERMISSION_DENIED | 无权限 |
| RESOURCE_NOT_FOUND | 资源不存在 |
| DEVICE_OFFLINE | 设备离线 |
| SENSOR_DATA_INVALID | 传感器数据异常 |
| ALGORITHM_FAILED | 算法执行失败 |
| DATASET_NOT_FOUND | 数据集不存在 |
| LLM_FAILED | 大模型调用失败 |
| DIGITAL_TWIN_DATA_ERROR | 三维数字孪生数据异常 |
| INTERNAL_ERROR | 服务内部错误 |

### 11.4 请求规范

- POST/PUT/PATCH 请求体必须使用 JSON，文件上传接口除外。
- 查询列表接口必须支持分页。
- 涉及时间范围的接口必须明确时区。
- 所有写操作必须记录操作者、操作时间和 request_id。
- 涉及算法任务的接口必须支持任务 ID 查询，不得要求前端长时间阻塞等待。

### 11.5 OpenAPI 文档

后端必须提供 OpenAPI 文档，并保证接口定义与实际实现一致。

建议地址：

```text
/api/openapi.json
/api/docs
```

## 12. 数据库与数据管理约束

项目根目录必须包含 `db/` 文件夹。

当前 canonical 初始化脚本为 `deploy/mysql/init.sql`，`db/` 目录用于治理说明、manifest、后续迁移和测试资产。建议结构：

```text
db/
  README.md
  manifest.json
  schema/
    README.md
  migrations/
  seed/
  test_data/
  er/
  backups/
```

若未来引入迁移制度，应从 `deploy/mysql/init.sql` 生成或迁移到明确版本文件；在真实文件落地前，不得把尚未创建的 schema 版本文件写成当前事实。

数据库至少应覆盖：

- 用户表。
- 角色权限表。
- 人员信息表。
- 传感器设备表。
- 传感器点位表。
- 传感器采样数据表。
- 小车设备表。
- 小车位置记录表。
- 小车 JPG/PNG 图片识别记录表；视频识别记录表属于后续扩展，未补齐验证集前不得写成已交付能力。
- YOLO 识别结果表。
- 泄漏场景表。
- 扩散模拟任务表。
- 扩散浓度场结果表。
- 溯源任务表。
- 溯源结果表。
- 逃生路径任务表。
- 告警日志表。
- 联动处置记录表。
- 算法模型版本表。
- 数据集元信息表。

所有表必须包含必要的创建时间、更新时间字段；涉及审计的数据必须包含创建人或来源字段。

## 13. 安全约束

1. API Key、MySQL 数据库密码、用户密码、token 密钥不得以明文形式暴露在代码、文档、提交记录或前端产物中。
2. 敏感配置必须通过环境变量、密钥管理服务或本地未提交配置文件注入。
3. 用户密码必须使用安全哈希算法存储，不得可逆加密，不得明文存储。
4. 前端不得保存长期有效的敏感密钥。
5. 日志中不得输出密码、token、API Key、身份证号、手机号等敏感信息。
6. 上传文件必须校验类型、大小和路径，禁止任意文件写入。
7. 所有管理接口必须有认证和权限控制。
8. 数据库账号应遵循最小权限原则。

## 14. 编码与可读性约束

1. 代码、数据库脚本、Markdown 文档、配置模板统一使用 UTF-8 编码。
2. Web 页面必须声明 UTF-8。
3. MySQL 必须使用 `utf8mb4` 字符集。
4. 所有内容编译、打包、部署后严禁出现中文乱码。
5. 命名必须清晰表达业务含义，禁止使用无意义变量名。
6. 文档必须能被新开发人员直接阅读理解，不得只写结论不写约束。
7. 重要算法参数、阈值、数据来源必须有说明。

### 14.1 无效代码与重复实现死规则

严禁为了图方便写入无效、重复、无意义或无法验证的代码、变量、函数、接口、文档和数据文件。任何新增内容都必须能说明用途、调用路径、输入输出、验证方式和失败边界；经不起代码审查、搜索去重、测试运行或业务流程验证的内容不得提交。

强制要求：

- 不得复制已有函数、接口、组件、模型入口或工具脚本后简单改名作为新实现。
- 不得新增同义重复变量、重复状态字段、重复 API 封装、重复路由入口、重复算法入口或重复文档。
- 不得保留未被调用、无业务意义、无数据来源、无验证命令、无维护责任的代码和文件。
- 不得用“临时先放着”“以后可能有用”“看起来完整”作为提交理由。
- 新增核心逻辑必须至少具备一种验证证据：类型检查、单元测试、接口测试、算法验证、构建验证、审计脚本或明确的人工复现步骤。
- 暂时无法验证的内容必须显式标记风险、原因、后续验证方法和责任范围，并不得在页面、接口或文档中表述为已完成能力。
- 代码审查时如发现无效、重复、无意义或不可验证内容，应先删除、合并或补齐验证证据，再允许合入。

## 15. GitHub 提交与仓库清洁约束

项目提交到 GitHub 前必须保持仓库干净、可读、可复现。多余文件、临时文件、不该公开的文件不得提交到 GitHub。

禁止提交以下内容：

- API Key、数据库密码、用户密码、token 密钥、私钥、证书、真实 `.env` 文件。
- 生产数据库备份、未脱敏数据库导出、真实用户数据、真实事故敏感数据。
- 运行日志、调试日志、错误堆栈日志、审计日志原始文件。
- `node_modules/`、`__pycache__/`、`.pytest_cache/`、`.venv/`、`dist/`、`build/`、`target/` 等依赖、缓存和构建产物。
- IDE 或本机环境文件，例如 `.idea/`、无必要的 `.vscode/` 私人配置、系统临时文件。
- 上传测试产生的图片、视频、压缩包、临时文件和无说明的大体积文件。
- 未注明来源、未脱敏、无授权的数据集。
- 未确认用途的模型权重、大体积模型文件和二进制文件。
- 与项目无关的截图、草稿、个人笔记、重复文档、废弃脚本。
- 包含中文乱码、无法打开、无维护价值或无法解释用途的文件。

提交约束：

- 必须维护 `.gitignore`，将缓存、日志、上传文件、密钥文件、构建产物和本地环境文件排除。
- 提交前必须检查 `git status`，确认只提交与本次任务相关的文件。
- 新增一级目录、重要脚本、数据集、模型文件时，必须有 README 或说明文档解释用途。
- 大体积文件应优先使用外部对象存储、模型仓库或 Git LFS，并在文档中说明获取方式。
- 不确定是否应提交的文件，默认不提交，先补充说明或人工确认。
- 不得为了“看起来完整”提交自动生成、重复、过期或无法维护的文件。
- GitHub 仓库应保持最小必要内容：源码、配置模板、数据库结构、文档、测试、部署说明和可复现实验说明。

## 16. Docker 与部署约束

需要经常维护、升级或独立运行的内容应放入 Docker 容器中。

建议容器化对象：

- Java 主后端服务。
- Python 算法服务。
- MySQL 数据库。
- 前端 Nginx 静态服务。
- Python 算法 API。
- YOLO 人员识别服务。
- 未来统一模型推理服务（未从当前 8000/8001 运行形态中拆出前，仅作为规划项）。
- 定时任务或数据采集服务。

部署时必须预留服务器端口，并支持部署到以下域名：

```text
www.cip.lab6119.xyz
```

建议端口规划：

| 服务 | 内部端口 | 外部建议 |
| --- | --- | --- |
| 前端 Web | 80 | 80/443 |
| Java 后端 API | 8081 | 内网转发 |
| Python 算法 API | 8000 | 8000 或内网转发 |
| MySQL | 3306 | 不建议公网暴露 |
| YOLO 人员识别服务 | 8001 | 内网访问 |
| 统一模型推理服务 | 8100 | 未来规划；当前仓库尚无独立 8100 容器或启动入口 |
| 文件服务 | 8200 | 按需开放 |

生产环境必须优先通过 HTTPS 访问，数据库不得直接暴露到公网。

## 17. 页面设计约束

系统至少包含以下页面：

1. 主页。
2. 三维园区。
3. 扩散与溯源。
4. 小车管理。
5. 监控数据管理。
6. 人员管理。
7. 404 页面。

### 17.1 主页

主页主要用于总体态势可视化，不承担复杂管理功能。

应展示：

- 园区总体安全状态。
- 当前告警数量。
- 四类气体浓度概览。
- 传感器在线率。
- 小车在线状态。
- 最近异常事件。
- 今日扩散/溯源任务统计。

### 17.2 三维园区

三维园区是项目核心展示页面。

应支持：

- 园区三维底座展示。
- 点位、设备、小车、泄漏点定位。
- 与二维平面或数据面板联动。
- 查看传感器仿真/采样数据及来源标识。
- 查看扩散浓度场和路径结果。

### 17.3 扩散与溯源

该页面是核心业务页面。

应支持：

- 设置泄漏点位。
- 设置气体类型和泄漏参数。
- 设置风速、风向等环境参数。
- 启动扩散模拟。
- 查看多点位浓度数据分布。
- 基于异常浓度数据进行溯源。
- 显示估计泄漏点、误差范围和置信度。
- 基于当前仿真浓度场生成并更新人员逃生路径。
- 形成异常日志和安全决策建议。

### 17.4 小车管理

小车管理页面应支持：

- 查看小车列表、在线状态、电量、位置和任务状态。
- 上传或接收小车摄像头 JPG/PNG 图片；视频识别作为后续扩展，不得冒充当前已交付能力。
- 调用 YOLO11m 识别人员位置。
- 查看小车搭载传感器检测到的危险气体浓度。
- 小车检测异常时，自动联动跳转到扩散与溯源页面。
- 将小车作为动态监控点位参与溯源计算。

### 17.5 监控数据管理

监控数据管理页面应支持：

- 查看固定传感器点位。
- 查看动态小车采样点位。
- 查询 CO、O2、NH3、CH4 浓度历史数据。
- 查看异常数据。
- 导出数据。
- 查看数据质量标记。

### 17.6 人员管理

人员管理页面应支持：

- 人员基础信息管理。
- 人员位置或区域信息管理。
- 角色权限管理。
- 应急联动联系人管理。
- 与逃生路径规划模块联动。

### 17.7 404 页面

系统必须提供统一 404 页面，用于处理不存在的路由、无效页面地址和已下线页面。

404 页面应满足：

- 保持与系统整体视觉风格一致。
- 明确提示当前页面不存在或地址无效。
- 提供返回主页、返回上一页、进入三维园区等基础操作。
- 不暴露后端路径、服务器异常堆栈、数据库信息或调试信息。
- 对未登录用户和已登录用户均可正常展示。
- 前端路由和服务器 Nginx/网关配置均应支持刷新后进入 404 页面，不应出现空白页。

## 18. 核心业务流程

### 18.1 泄漏扩散与溯源流程

```text
创建泄漏场景
  -> 设置环境参数和气体参数
  -> 扩散模型生成浓度场
  -> 固定传感器/小车点位获得浓度数据
  -> 异常浓度识别
  -> 溯源模型反演泄漏点
  -> 输出泄漏点估计结果
  -> 三维/二维可视化展示
  -> 生成异常日志和联动建议
```

### 18.2 小车异常联动流程

```text
小车巡检
  -> 摄像头上传 JPG/PNG 图片
  -> YOLO11m 识别人员位置
  -> 传感器检测气体浓度
  -> 发现异常浓度
  -> 自动记录异常日志
  -> 跳转扩散与溯源页面
  -> 汇总全部监控点位浓度数据
  -> 溯源泄漏点
  -> 触发安全决策和路径规划
```

### 18.3 逃生路径流程

```text
获取人员位置
  -> 获取安全出口
  -> 获取道路/可通行区域拓扑
  -> 获取当前浓度场
  -> 剔除高危区域
  -> D* Lite 计算最优路径
  -> 气体扩散变化后动态重规划
  -> 二维优先展示，三维同步预留
```

### 18.4 千问大模型辅助决策流程

千问大模型后续可作为系统的辅助决策与知识问答模块接入，但必须基于扩散模型、溯源模型、路径规划模型、传感器数据和应急规范文档提供建议。

建议流程：

```text
获取当前事故上下文
  -> 汇总气体类型、浓度、泄漏估计点、人员位置、风速风向、告警等级
  -> 检索应急预案、国家标准、企业处置规范和历史案例
  -> 千问大模型生成逃生规范建议、事故处理建议和沟通话术
  -> 后端标注建议来源、可信边界和人工复核要求
  -> 前端展示为辅助建议，不作为强制指令
  -> 处置人员确认后形成联动记录
```

可接入场景：

- 逃生规范建议：根据气体类型、浓度、人员位置和安全出口，生成面向人员的疏散提醒。
- 事故处理建议：根据泄漏类型、风险等级和现场条件，生成处置步骤建议。
- 应急预案问答：基于本地预案文档、国家标准和企业制度回答问题。
- 异常数据解释：解释某段时间内传感器异常、浓度突变或小车采样异常的可能原因。
- 事故报告辅助生成：根据告警日志、传感器数据、溯源结果和处置记录生成初稿。
- 值班人员辅助研判：将复杂图表、浓度趋势、路径变化转换为更容易理解的文字说明。
- 培训与演练：根据历史事故场景生成演练问答、考核题和处置复盘材料。
- 接口与日志分析：辅助研发人员分析接口异常、模型调用失败和数据质量问题。

大模型接入限制：

- 不得直接控制小车、告警、门禁、广播或任何现场执行设备。
- 不得绕过扩散、溯源和路径规划算法直接给出“确定性”结论。
- 涉及安全处置的输出必须显示“辅助建议，需人工复核”。
- 所有大模型请求和响应必须记录 request_id、模型名称、模型版本、输入摘要和输出摘要。
- 不得向大模型发送未脱敏的用户密码、API Key、数据库密码或无关个人敏感信息。
- 大模型不可用时，系统核心监测、扩散、溯源、路径规划和告警功能必须仍可运行。

## 19. 测试与验收约束

### 19.1 功能测试

每个核心页面必须有基本功能验证：

- 页面能正常加载。
- API 调用成功与失败均有处理。
- 核心操作有明确反馈。
- 异常状态可见。
- 数据为空时有合理展示。

### 19.2 接口测试

每个接口至少验证：

- 正常参数。
- 缺失参数。
- 非法参数。
- 无权限访问。
- 服务异常。

### 19.3 算法测试

核心算法至少完成：

- 5 次真实性验证。
- 误差指标记录。
- 失败案例记录。
- 与基线方法对比。
- 参数敏感性说明。

### 19.4 部署测试

部署前必须验证：

- Docker 容器可启动。
- 数据库初始化可执行。
- 前端可访问。
- API 文档可访问。
- 核心接口可调用。
- 中文无乱码。
- 敏感信息未暴露。

## 20. 客观性与风险表达约束

系统所有算法结果、告警结果和安全建议必须保持客观。

禁止表达：

- “绝对准确定位泄漏点”。
- “完全保证人员安全”。
- “模型结果一定正确”。

推荐表达：

- “模型估计泄漏点位为……”
- “在当前输入参数和数据质量条件下……”
- “本结果需结合现场情况复核。”
- “该路径为当前浓度场下的推荐路径，后续将随扩散变化更新。”

## 21. 文档与交付物约束

项目至少应维护以下文档：

```text
README.md
docs/项目总体要求.md
docs/api-reference.md
docs/architecture.md
docs/coding-standards.md
docs/development-guide.md
docs/algorithm-verification-report.md
docs/dataset-sources.md
deploy/README.md
db/README.md
```

所有文档必须保持与代码一致。接口、数据库表、算法参数、部署端口发生变化时，必须同步更新相关文档。

## 22. 最小验收标准

项目达到以下条件才可认为完成首期可用版本：

1. 前端 6 个核心业务页面和 404 页面均可访问，核心流程可串联。
2. 后端 API 遵循统一响应协议。
3. `db/` 文件夹存在并包含治理 manifest；数据库初始化脚本以 `deploy/mysql/init.sql` 为 canonical source。
4. MySQL 可初始化并保存核心业务数据。
5. 四类气体浓度数据可采集、存储、查询和展示。
6. 扩散模型可运行，并能输出二维浓度场。
7. 溯源模型可根据多个点位浓度数据输出泄漏点估计结果。
8. D* Lite 可根据浓度风险动态生成逃生路径。
9. 小车管理页面可处理 JPG/PNG 图片上传并调用 YOLO11m 识别；视频识别尚未交付，不得在演示或文档中冒充已支持。
10. 至少完成 5 次算法真实性验证并形成报告。
11. Docker 化服务可启动。
12. 敏感信息不以明文形式出现在代码中。
13. 编译、运行、部署后不出现中文乱码。
14. 具备部署到 `www.cip.lab6119.xyz` 的端口和配置预留。

## 23. 后续扩展方向

后续可逐步扩展：

- 三维气体体渲染。
- 更精细的 CFD 模型校准。
- 多源异构传感器融合。
- 小车自动巡检任务规划。
- 多人员动态疏散仿真。
- 与应急指挥系统、短信、电话、企业微信或政务平台联动。
- 基于历史数据的风险预测。
- 传感器点位布局自动优化。
