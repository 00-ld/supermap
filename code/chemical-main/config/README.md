# Config

This directory stores configuration templates and schema notes.

Rules:
- Commit examples only, never real secrets.
- Use environment variables for API keys, MySQL passwords, user passwords, and tokens.
- Keep production-only values outside Git.
- Document required variables and safe defaults in template files.
