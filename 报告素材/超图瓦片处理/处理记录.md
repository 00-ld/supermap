# SuperMap 三维瓦片处理记录

记录时间：2026-07-13

## 1. 处理目标

将 `G:\竞赛\超图杯\园区大屏部署\瓦片` 下已有 S3M/SCP 三维瓦片整理为后续 iDesktopX 检查、iServer 发布、iPortal 管理和前端 iClient3D 加载可复用的资源包。

本次处理遵循“不破坏原始数据”的原则：原始瓦片目录只读检查，所有清单、截图和报告素材写入 `G:\竞赛\超图杯\报告素材\超图瓦片处理`。

## 2. 桌面端环境

- 使用软件：SuperMap iDesktopX 2026 试用版。
- 启动方式：用户已启动桌面端 2026 版本。
- 环境截图：`screenshots/01-idesktopx-2026-main.png`。

## 3. 已识别瓦片资源

详见：

- `manifests/s3m-tile-manifest.json`
- `manifests/s3m-tile-manifest.txt`

当前识别到 6 组 SCP 三维瓦片：

| 序号 | 资源类型 | 名称 | SCP 文件 |
|---|---|---|---|
| 1 | 厂房 | 加工厂房 | `园区大屏部署\瓦片\厂房三维瓦片\加工厂房\result_ImportFBX\result_ImportFBX.scp` |
| 2 | 厂房 | 原材料仓库 | `园区大屏部署\瓦片\厂房三维瓦片\原材料仓库\result_ImportFBX.scp` |
| 3 | 厂房 | 生产装置厂房 | `园区大屏部署\瓦片\厂房三维瓦片\生产装置厂房\result_ImportFBX\result_ImportFBX.scp` |
| 4 | 设备 | 换热器 | `园区大屏部署\瓦片\设备三维瓦片\换热器\换热器.scp` |
| 5 | 设备 | 立式罐子 | `园区大屏部署\瓦片\设备三维瓦片\立式罐子\罐子.scp` |
| 6 | 设备 | 蒸馏塔 | `园区大屏部署\瓦片\设备三维瓦片\蒸馏塔\蒸馏塔.scp` |

## 4. 技术观察

- SCP 文件均为 SuperMap S3M 缓存描述文件。
- `dataType` 为 `ArtificialModel`，适合按人工模型/白模资源进入三维场景。
- 当前 `crs` 为 `epsg:0`，属于平面坐标系统，发布到真实园区三维场景前需要确认坐标配准或在场景中统一定位。
- 厂房模型和设备模型应分层发布，便于前端按业务含义控制显隐、拾取和高亮。

## 5. 后续处理步骤

1. 在 iDesktopX 2026 中逐个加载 SCP 瓦片。
2. 检查模型是否能正常显示，确认纹理、范围、层级和坐标状态。
3. 对每组瓦片截图，形成报告证据。
4. 形成 iServer 发布清单：图层名称、SCP 源路径、建议服务名、前端加载 URL。
5. 将前端三维入口改造为 iClient3D 原生加载优先，iPortal 大屏作为备用。


## 6. iClient3D 本地预览服务修正

- 处理时间：2026-07-13 12:58:49
- 对 supermap-s3m-preview-server.mjs 增加请求日志 preview-requests.log，并补充 /tiles/<layer>/config 到 SCP 文件的映射，模拟 iServer 三维服务 config 地址。
- 对 supermap-s3m-preview.html 增加 EPSG:0 平面瓦片定位逻辑，隐藏地球底图，按 SCP geoBounds / heightRange / position.point3D 设置相机。
- 目的：让本地 SCP 缓存以接近 iServer 发布后的 URL 形态进行预览验证，并保留网络请求证据。


## 7. 浏览器验证路径记录

- 处理时间：2026-07-13 13:00:54
- 优先尝试 Codex 内置浏览器访问 http://127.0.0.1:8098/，页面接管超时：Timed out waiting for the Browser webview to attach。
- 根据前端验证流程，改用本机 Microsoft Edge + Playwright 进行本地 WebGL 预览截图。


## 8. iClient3D 许可探测路径补齐

- 处理时间：2026-07-13 13:05:30
- 请求日志显示缺失 /manager/license.json，这是 iClient3D 在本地站点根路径探测服务许可的固定请求。
- 在报告素材目录增加 manager-license.json 空对象，并通过预览服务映射 /manager/license.json，避免本地静态预览因 404 中断。

## 9. 本地 SCP 属性文件路径兼容

- 处理时间：2026-07-13 13:15
- 重新测试直连 SCP 后，日志显示运行时请求 `result_ImportFBX.scpattribute.json`，未进入 `.s3mb` 几何瓦片请求。
- 检查源目录确认存在 `attribute.json` / `attribute.db`。
- 在预览服务中将 `*.scpattribute.json`、`data/path/attribute.json` 映射到瓦片目录根部 `attribute.json`，将对应 `.db` 映射到 `attribute.db`。

## 10. 空场景截图作废说明

- 处理时间：2026-07-13 13:25
- `30-iclient3d-processing-factory.png`、`31-edge-processing-factory.png`、`32-edge-processing-factory-after-license.png`、`33-edge-processing-factory-debug-state.png`、`34-edge-processing-factory-geocamera.png`、`35-edge-processing-factory-direct-scp.png`、`36-edge-processing-factory-fixed-8099.png`、`37-edge-processing-factory-flyto.png` 只作为 WebGL 路径排障证据。
- 这些截图没有出现三维模型主体，不能作为“三维场景已加载”的报告截图。
- 后续报告有效截图必须满足：iDesktopX 或 iClient3D 画面中可见厂房/设备模型主体，并带有图层或处理步骤上下文。

## 11. iDesktopX 桌面端加载进度

- 处理时间：2026-07-13 13:30
- 在 iDesktopX 的“打开图层文件”对话框中打开 `化工园区场景.scp`。
- 打开后左侧图层树出现 `化工园区场景`，说明 SCP 已加入当前场景。
- 当前视口仍为蓝底，属于“图层已加载但相机未定位到模型”状态，截图 `42-idesktop-enter-open-scp-result.jpg` 仅作为中间过程，不作为最终有效三维场景图。

## 12. iDesktopX 有效三维场景截图

- 处理时间：2026-07-13 13:35
- 读取 `化工园区场景.scp`，确认中心点 `X=-397.75005895327922`、`Y=-125.91957235375594`，高度范围 `-20.055293910112766` 至 `170.87648119210795`。
- 在 iDesktopX “场景定位”中输入 `X=-397.750059`、`Y=-125.919572`、`Z=500`，定位到园区中心。
- `47-idesktop-locate-key-result.jpg`：有效俯视场景截图，可见园区道路、厂房、罐区、管廊和设备模型。
- `48-idesktop-current-after-user-input.jpg`：有效斜视三维场景截图，可见园区三维模型主体、设备高度和空间关系，后续报告优先使用。

## 13. Web 端 SuperMap3D 原生入口验证

- 处理时间：2026-07-13 14:00-14:30。
- iServer 查询确认三维服务：`3D-local3DCache-HuaGongYuanQuChangJing/rest`。
- iServer 查询确认三维数据名：`化工园区场景`。
- Web 端使用 SuperMap3D / iClient3D 原生 Viewer 加载 iServer S3M config：
  `.../rest/realspace/datas/化工园区场景/config`。
- SuperMap3D SDK 通过前端同源代理 `/supermap3d-remote` 加载，避免远程 SDK Worker 跨源拦截。
- Playwright 验证结果：
  - `51-web-screen-supermap3d-ready.png`：状态已进入“原生三维已加载”，但画面仍偏暗，不作为最终报告主图。
  - `53-web-screen-supermap3d-ready-model.png`：有效 Web 端三维入口截图，可见园区道路、厂房、罐区和设备模型；页面状态为“原生三维已加载”；算法封装状态为 `chemical-algorithm 3.0.0` 在线。
  - `53-web-screen-supermap3d-ready-model.json`：记录页面状态、canvas 尺寸、SuperMap3D 运行时状态和 S3M 瓦片请求尾部证据。
- 网络证据：验证过程中多条 `.s3mb` 瓦片、`config`、`attribute.json` 请求返回 200，说明 Web 端已从 iServer 拉取真实三维瓦片，不是空 WebGL 画布。

## 14. Web 端二维地图 SuperMap iClient2D 接入

- 处理时间：2026-07-13 15:00-15:25。
- 目标：`/smart-map` 二维智慧地图也使用 SuperMap 产品链，而不是仅在三维 `/screen` 使用 SuperMap。
- 实现方式：
  - 新增 `frontend/src/views/smart_map/components/SuperMap2DLayer.vue`。
  - 采用 `@supermap/iclient-leaflet` + `leaflet` 加载 iServer REST 二维地图服务。
  - SuperMap 二维底图置于现有 Canvas 业务图层下方，Canvas 保留扩散模拟、溯源、传感器选点、疏散规划等现有算法交互。
  - `useSmartMapRenderer.ts` / `useSmartMapBaseCanvas.ts` 将原本不透明底色改为半透明覆盖，确保 SuperMap 底图可见。
- 服务选择：
  - 初始测试 `map-china/rest/maps/ChinaDark` 的 JSON 元信息可访问，但瓦片和 `image.png` 出图请求返回 `502 Bad Gateway`，不能作为当前有效底图。
  - 改用稳定可出图的 `map-world/rest/maps/World`，EPSG:4326，浏览器验证 16 张瓦片全部加载成功。
- 有效截图：
  - `screenshots/54-web-smartmap-supermap2d-ready.png`：Web 端 `/smart-map` 二维智慧地图，状态为 `SuperMap iClient2D 已加载`，可见 iServer `World` 底图叠加园区业务 Canvas。
  - `screenshots/55-web-smartmap-supermap2d-real-backend.png`：后端、前端、算法服务均启动后复核的二维智慧地图截图，16/16 张 SuperMap iServer 瓦片加载成功，优先作为报告截图。
- 验证命令：
  - `npm run typecheck` 通过。
  - `npm run build` 在系统 Node 25.2.1 下于 Vite transform 后进程异常退出；切换到 Codex bundled Node 24.14.0 后构建通过。
- 事实边界：
  - 当前二维底图是 iServer `map-world/World` 通用地图服务，不是已发布的园区专用二维地图服务。
  - 园区设施、传感器、扩散、溯源和疏散仍由前端 Canvas 业务图层表达；后续若发布园区二维矢量/栅格服务，可将 `VITE_SUPERMAP_2D_MAP_URL` 切换为园区专用服务。

## 15. 本地系统启动状态

- 处理时间：2026-07-13 15:25。
- 前端：`http://127.0.0.1:5173/` 已运行。
- 算法服务：`http://127.0.0.1:8000/api/health` 返回 200。
- Docker Desktop：已启动。
- MySQL：Docker 容器 `chemical-local-mysql` 已启动，`chemical` 库表结构已存在。
- Java 后端：Spring Boot 已启动并监听 `8081`，业务接口未携带登录 token 时返回 401，属于鉴权预期行为。
- Web 复核：使用本地有效 token 进入 `/smart-map`，SuperMap iClient2D 状态为 ready；截图 `55-web-smartmap-supermap2d-real-backend.png`。
- 可选 YOLO/图像分析服务 `8001` 未启动，页面中可能出现相关 503 请求；这不影响二维/三维 SuperMap 场景、扩散模拟 API 和主后端运行。

## 16. 三维场景 WGS84 球面坐标修正

- 处理时间：2026-07-13 17:20。
- 已重新请求 iServer S3M config，确认当前三维缓存仍为 `crs=epsg:0`，XML 内为 `Planar Coordinate System---m`，不能写成瓦片本体已经具备真实地理坐标系。
- Web 端短期修正：
  - S3M 图层加载时通过 SuperMap S3M `position` 参数设置 WGS84 插入点 `118.788410E, 32.035746N, 29.56m`。
  - `/screen` 默认相机改为园区中心经纬度相机 `118.788410E, 32.035746N, 2600m`，算法定位按钮仍可定位源点 `118.784398E, 32.036946N`。
  - 扩散、粒子溯源和疏散规划结果继续通过 `worldToGeo()` 转为 WGS84，再用 `Cartesian3.fromDegrees` 叠加到 SuperMap 三维场景。
  - Vite 增加 `/supermap-iserver` 同源代理，避免 iClient3D 请求 `license.json`、`login.json`、`config` 时被浏览器 CORS 拦截。
- 本次修改后的验证：
  - `npm run typecheck:strict` 通过。
  - `npm run build` 通过。
  - `/screen` 页面可显示“原生三维已加载”、iServer 图层数 `1`、WGS84 源点/插入点，扩散算法能返回 requestId、耗时和峰值。
- 事实边界：
  - 该步骤保证 Web 端模型插入点、相机和算法结果都有经纬度入口，并落在球面坐标表达中。
  - 但 `57-web-screen-wgs84-native-diffusion.png`、`58-web-screen-wgs84-proxied-s3m-diffusion.png`、`59-web-screen-after-focus-park.png`、`60-web-screen-after-focus-geo.png`、`61-web-screen-wgs84-center-camera-diffusion.png` 仍未出现三维模型主体，只能作为坐标链路和排障证据，不能作为最终三维成果图。
  - 当前网络只稳定请求到 `login.json`、`license.json`、`config`、`attribute.json`，未稳定触发 `.s3mb` 几何瓦片请求；这与 EPSG:0 平面缓存临时插入球面坐标的边界有关。
  - 若报告需要写“三维瓦片本体具有真实坐标系”，必须后续在 iDesktopX 中用真实控制点/坐标系重处理并重新生成 S3M/SCP 缓存。

## 17. 下一步真实坐标系处理要求

要满足“真实三维坐标系、落在球面、有经纬度”，后续 iDesktopX 必须完成：

1. 确认园区真实控制点，至少 2 个，推荐 3 个以上：园区西北角、东南角、主入口或明显建筑角点。
2. 明确目标坐标系：优先 CGCS2000 / WGS84，经纬度以度为单位，高程以米为单位。
3. 在 iDesktopX 中对当前 EPSG:0 三维模型/SCP 做地理参考或坐标转换，记录原始点 `X/Y/Z` 与目标 `经度/纬度/高程` 对应关系。
4. 重新生成 S3M/SCP 缓存，要求新 config 中 `crs` 不再是 `epsg:0`，或至少服务元数据能解释真实 CRS/插入点。
5. 重新发布 iServer Realspace 服务，Web 端继续使用 `/supermap-iserver` 同源代理加载。
6. 复核标准：浏览器网络必须出现 `.s3mb` 请求 200，截图中必须可见模型主体，同时页面显示 WGS84 源点和算法证据。

## 18. Web 三维绿屏/空场景修复

- 处理时间：2026-07-13 20:35-21:35。
- 问题表现：
  - `/screen` 长时间显示绿屏或空星空背景。
  - 页面状态可能显示“原生三维已加载”，但网络没有稳定触发 `.s3mb` 几何瓦片请求，截图不能作为最终成果图。
- 已定位原因：
  - 当前 S3M config 仍为 `epsg:0` 平面米制缓存，前端强行传 WGS84 `position` 会导致模型与相机/瓦片调度脱节。
  - `scene.open()` 从 `/supermap-iserver/.../realspace` 进入后，会继续派生根路径 `/iserver/.../scenes/默认场景.json` 和 `/iserver/.../layers.json` 请求；Vite 原来没有代理 `/iserver`，这些请求被前端 dev server 误返回 HTML，导致后续三维瓦片调度异常。
- 已修改：
  - `frontend/.env.development`：SuperMap3D SDK 切回已验证可加载平面 S3M 的 `/supermap3d-remote/iportal/apps/earth/v2/SuperMap3D/SuperMap3D.js`。
  - `frontend/.env.development`：新增并默认关闭 `VITE_SUPERMAP_3D_APPLY_LAYER_POSITION=false`。
  - `frontend/vite.config.ts`：新增 `/iserver` 根路径代理到 iServer，保留 `/supermap-iserver` 代理。
  - `frontend/src/components/SuperMapSceneViewer.vue`：恢复 `scene.open(autoSetView)` 优先加载；S3M config 只作兜底；默认不再用 WGS84 `position` 强行移动 EPSG:0 图层。
- 验证结果：
  - `npm run typecheck:strict` 通过。
  - `screenshots/68-web-screen-scene-open-autoview-model.png`：有效 Web 三维截图，可见厂房、道路、管廊和三维模型主体。
  - `screenshots/68-web-screen-scene-open-autoview-model.json`：记录 `.s3mb` 请求 84 条、SuperMap3D runtime、canvas 尺寸和页面状态。
- 事实边界：
  - 这次修复解决的是 Web 端可见三维模型和同源代理链路，不表示三维瓦片本体已具备真实 WGS84/CGCS2000 坐标系。
  - 旧工程中存在 OSM/业务数据的 CGCS2000(EPSG:4490) 输出和数据库经纬度双写设计，可作为算法业务点位经纬度来源；但未找到能直接证明当前 S3M 瓦片本体已地理配准的三维控制点台账。

## 19. 二维分析引擎与粒子 KDE 概率地形调整

- 处理时间：2026-07-13 22:20-23:05。
- 新架构口径：
  - 二维地图不能去掉，必须加强为“后台计算引擎”。
  - 二维 iClient2D / iServer Data 承担道路网络、缓冲区、叠加分析、空间查询和路径分析。
  - 三维 iClient3D 只作为“前台投影仪”，负责把扩散云团、KDE 概率地形、置信椭圆和疏散路线抬高渲染到 S3M 场景。
- 已修改：
  - `algorithm/inversion/particle_filter.py`：在粒子滤波任务返回阶段，对最终 `particles + weights` 做加权 Gaussian KDE，输出 `posteriorDensityGeoJSON`。
  - `posteriorDensityGeoJSON` 为规则 Polygon FeatureCollection，每个格网面带 `normalizedDensity`、`elevationZ` 和三维坐标 `[x,y,z]`。
  - `frontend/src/components/SuperMapSceneViewer.vue`：删除前端合成的粒子概率云团和粒子过程轨迹；只消费后端 KDE GeoJSON 渲染概率地形。
  - 若后端没有返回 KDE GeoJSON，三维端只显示最终估计点和 95% 置信椭圆，不展示粒子滤波过程。
  - `readme.md`、`code/chemical-main/README.md`、`docs/supermap-cup-implementation-ledger.md`：同步更新二维/三维分工边界。
- 验证结果：
  - `python -m py_compile algorithm/inversion/particle_filter.py algorithm/api_server.py` 通过。
  - `npm run typecheck:strict` 通过。
  - `python -m unittest algorithm.tests.test_algorithm_endpoints_contract.AlgorithmEndpointsContractTests.test_particle_filter_returns_posterior` 通过。
  - 真实 HTTP 抽测 `POST http://127.0.0.1:8000/api/inversion/particle-filter` 通过，返回 `posteriorDensityGeoJSON.type=FeatureCollection`、`featureCount=144`、`metadata.interpolation=weighted-gaussian-kde`，样例格网面包含 Z 值。
- 事实边界：
  - 当前 KDE GeoJSON 坐标仍是算法园区平面坐标，三维前端按 EPSG:0 本地场景坐标映射到当前 S3M。
  - 后续若 iDesktopX 完成真实 CGCS2000/WGS84 重缓存，前端保留 WGS84 分支即可把同一结果链路切到真实球面坐标。
  - 疏散路径长期应从二维道路网络数据集/iServer 网络分析模块返回坐标串，三维只给路径点赋固定 Z 值后渲染。

## 20. 道路网络与建筑/设施数据集识别

- 处理时间：2026-07-13 22:55-23:15。
- 数据来源：
  - `frontend/src/data/realMapAssets.js`：已有 DOM 人工识别道路、建筑/设施区、园区出入口、建筑边界点。
  - 独立 SCP 元数据：加工厂房、生产装置厂房、原材料仓库、换热器、立式罐子、蒸馏塔，用 `geoBounds/heightRange` 生成 S3M 对象足迹。
- 已生成目录：
  - `报告素材/二维数据集识别/supermap_import/`
  - `报告素材/二维数据集识别/supermap_udbx/`
- 已生成矢量数据：
  - 道路面：`road_polygons_map.geojson`，15 条。
  - 道路网络节点：`road_network_nodes_map.geojson`，69 个。
  - 道路网络边：`road_network_edges_map.geojson`，93 条，已按交叉口拆边，包含 `fromNode/toNode/length_m/main` 字段。
  - 建筑/设施区面：`building_facility_polygons_map.geojson`，10 个，字段标注 `precisionLevel=facility_zone`、`needsDesktopRefinement=true`。
  - 出入口点：`entrance_points_map.geojson`，14 个。
  - S3M 对象足迹：`s3m_object_footprints_map.geojson`，6 个。
- SuperMap 数据源：
  - 使用 iObjectsPy 12.1.0.0 成功导入为 `报告素材/二维数据集识别/supermap_udbx/chemical_park_vectors.udbx`。
  - 数据集名称：
    - `Park_RoadPolygon_R`
    - `Park_RoadNetworkNode_P`
    - `Park_RoadNetworkEdge_L`
    - `Park_BuildingFacilityPolygon_R`
    - `Park_EntrancePoint_P`
    - `Park_S3MObjectFootprint_R`
- QA 图：
  - `报告素材/二维数据集识别/dataset_qa_overlay.png`
  - 图层含义：黄色主路、青色道路网络边、蓝色建筑/设施区、橙色 S3M 对象足迹、绿点园区出入口、红点建筑/设施入口。
- 验证结果：
  - `node tools/supermap/export-vector-datasets.mjs` 成功。
  - iObjectsPy 首次导入失败，原因是命令行 PATH 未包含 iDesktopX `bin`，Java 网关找不到 `WrapjCore.dll`。
  - 临时加入 iDesktopX `bin` 后，`python tools/supermap/import-vector-datasets-iobjectspy.py` 成功生成 UDBX。
  - 二次使用 iObjectsPy 打开 `chemical_park_vectors.udbx` 成功，确认包含 6 个数据集：`Park_RoadPolygon_R`、`Park_RoadNetworkNode_P`、`Park_RoadNetworkEdge_L`、`Park_BuildingFacilityPolygon_R`、`Park_EntrancePoint_P`、`Park_S3MObjectFootprint_R`。
- 事实边界：
  - 当前道路网络可以作为二维路径分析底稿，但还需在 iDesktopX 中检查拓扑连通性、重复边和断头路。
  - 当前建筑数据是“建筑/设施区面”，不是所有单体建筑的最终精确轮廓；后续需对照三维模型和 DOM 在 iDesktopX 中逐栋精修。
  - 当前坐标仍服务 EPSG:0 平面米制模型贴合；真实 CGCS2000/WGS84 发布需等控制点和重投影完成。

## 21. 二维数据集桌面端校核与拓扑校核

- 处理时间：2026-07-13 23:20-23:35。
- 桌面端状态：
  - 已通过 Computer Use 识别到正在运行的 `SuperMap iDesktopX 2026-「试用版」` 窗口。
  - 截图捕获失败，系统返回 `StartCapture failed: 已禁用桌面合成 (0x80263001)`；因此没有继续盲点 iDesktopX 菜单，避免误操作。
  - iDesktopX 可访问性树只暴露标题栏/系统菜单，无法可靠通过 UI 树完成图层勾选和截图校核。
- 已完成的可复现校核：
  - 使用 iObjectsPy 打开 `报告素材/二维数据集识别/supermap_udbx/chemical_park_vectors.udbx` 成功。
  - 确认数据源内包含 6 个数据集：道路面、道路节点、道路边、建筑/设施面、出入口点、S3M 对象足迹。
  - 运行 `node tools/supermap/validate-vector-datasets.mjs` 生成：
    - `报告素材/二维数据集识别/dataset_validation_report.json`
    - `报告素材/二维数据集识别/dataset_validation_report.md`
- 校核结论：
  - 道路网络节点：69 个。
  - 道路网络边：93 条。
  - 道路网络连通分量：1，说明当前道路网络整体连通。
  - 孤立节点：0。
  - 断头节点：30，主要为园区边界和道路末端，后续可按出入口/封闭端分类。
  - 道路边总长度：11651.8 m。
  - 边字段问题：0，`fromNode/toNode/length_m` 与几何一致。
  - 出入口校核：14 个出入口均在道路中心线 25m 吸附阈值内。
- 事实边界：
  - 这次已经完成“可计算道路网络”的拓扑校核，可作为二维路径分析底稿。
  - 建筑数据仍为设施区面，需在 iDesktopX 中对照三维模型进一步拆成单体建筑轮廓后再作为最终建筑数据集发布。
  - 若需要桌面端截图，需先恢复 Windows 桌面合成或在 iDesktopX 内手工导出地图窗口截图。

